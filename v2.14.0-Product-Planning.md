# v2.14.0 产品规划 - 脚本模板系统前端UI

**版本号**: v2.14.0 (或 v2.13.1)  
**功能**: 脚本模板系统前端UI开发  
**规划时间**: 2026-04-12  
**状态**: 📋 待审批

---

## 📊 版本号决策

### 选项1: v2.13.1（小版本更新）
**理由**: 是v2.13.0的功能补全，不是新功能  
**优势**: 版本号清晰表明是模板系统的完善  
**劣势**: 可能被视为bug修复而非feature

### 选项2: v2.14.0（中版本更新）⭐ 推荐
**理由**: 前端UI是完整的功能交付，用户可见价值高  
**优势**: 独立的版本号，便于发布说明和用户沟通  
**劣势**: 版本号增长较快

**建议**: 采用 v2.14.0，因为这是用户可见的完整功能交付

---

## 🎯 背景与动机

### 当前状态

**v2.13.0已完成**:
- ✅ 后端API（7个endpoints）
- ✅ 数据库（script_templates表）
- ✅ 变量替换引擎
- ✅ A/B脚本生成
- ✅ 3个预置模板
- ✅ 核心功能测试通过

**用户痛点**:
- ❌ 用户无法通过UI使用模板功能
- ❌ 无法可视化浏览和选择模板
- ❌ 无法通过界面填写变量
- ❌ 无法将脚本保存为模板（需要API调用）

**业务影响**:
- 后端功能已完成，但用户无法使用
- 预估168倍ROI无法实现
- 开发投入（6小时）未产生实际业务价值

### 用户需求

**核心用户故事**:

1. **作为内容策划师**，我希望能在Templates页面浏览所有可用模板，以便快速找到合适的脚本结构
2. **作为品牌营销人员**，我希望能预览模板的详细信息和变量列表，以便判断是否适合我的产品
3. **作为投流运营**，我希望能通过表单填写变量并一键生成A/B脚本，以便快速创建投放内容
4. **作为内容创作者**，我希望能将优秀的脚本保存为模板，以便后续复用和分享

---

## 🎨 功能设计

### 1. Templates页面（模板库）

**页面路径**: `/templates`  
**功能**: 模板浏览、搜索、筛选、查看详情

#### 1.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│ 超级洞察 > Templates                         [创建模板] │
├─────────────────────────────────────────────────────────┤
│ 🔍 搜索模板...     [全部分类▼] [全部平台▼]            │
│                                                         │
│ ┌───────┐ ┌───────┐ ┌───────┐                        │
│ │情感型 │ │理性型 │ │种草型 │ + 2个                   │
│ │  1    │ │  1    │ │  1    │                         │
│ └───────┘ └───────┘ └───────┘                        │
│                                                         │
│ ┌─────────────────────────────────────────┐            │
│ │ TemplateCard                            │            │
│ │ 情感共鸣型-室友对比                       │ [官方]   │
│ │ 通过室友对比引发共鸣，适合学生/年轻人群体     │            │
│ │ 14个变量 · 5个分段 · 使用23次              │ [预览] │
│ └─────────────────────────────────────────┘            │
│                                                         │
│ ┌─────────────────────────────────────────┐            │
│ │ TemplateCard ...                        │            │
│ └─────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

#### 1.2 核心组件

**TemplateCard组件**:
```typescript
interface TemplateCardProps {
  template: ScriptTemplate
  onPreview: (id: string) => void
  onApply: (id: string) => void
}
```

**功能**:
- 显示模板名称、描述、分类标签
- 显示变量数量、分段数量、使用次数
- "官方"徽章（project_id=NULL）
- 悬停效果（与TopicCard一致）
- 点击预览/应用按钮

#### 1.3 筛选和搜索

**筛选条件**:
- 分类：情感型/理性型/种草型/自定义
- 平台：抖音/快手/小红书
- 范围：全部/全局模板/项目模板

**搜索**:
- 实时搜索（debounce 300ms）
- 匹配字段：name/description/tags
- 高亮匹配关键词

### 2. TemplateDetailModal（模板详情弹窗）

**触发**: 点击TemplateCard的"预览"按钮

#### 2.1 弹窗布局

```
┌───────────────────────────────────────────────────────┐
│ 情感共鸣型-室友对比                         [×]        │
├───────────────────────────────────────────────────────┤
│ 📋 模板信息                                            │
│ 描述：通过室友对比引发共鸣，适合学生/年轻人群体           │
│ 分类：情感型  平台：抖音  使用次数：23                  │
│                                                       │
│ 🎯 脚本结构（5个分段）                                 │
│ ┌─────────────────────────────────────────┐          │
│ │ 1. Hook (0-3s)                          │          │
│ │ 室友花{价格_高}买的{产品类别}...          │          │
│ │ 镜头：宿舍场景，两瓶产品对比特写              │          │
│ └─────────────────────────────────────────┘          │
│ ... (其他分段)                                        │
│                                                       │
│ 📝 变量列表（14个）                                   │
│ 价格_高、价格_低、产品类别、产品名、核心卖点...          │
│                                                       │
│ ┌────────┐  ┌────────┐                              │
│ │ 使用模板 │  │ 取消   │                              │
│ └────────┘  └────────┘                              │
└───────────────────────────────────────────────────────┘
```

#### 2.2 交互流程

1. 用户点击"使用模板"按钮
2. 打开VariableFormModal（变量填写弹窗）
3. 用户填写变量
4. 提交后生成A/B脚本并跳转到Scripts页面

### 3. VariableFormModal（变量填写弹窗）

**触发**: TemplateDetailModal点击"使用模板"

#### 3.1 弹窗布局

```
┌───────────────────────────────────────────────────────┐
│ 填写模板变量                                 [×]        │
├───────────────────────────────────────────────────────┤
│ 基础信息                                              │
│ ┌────────────────────────┐                          │
│ │ 选择选题: [下拉选择]     │ *必填                    │
│ └────────────────────────┘                          │
│                                                       │
│ 模板变量（14个）                                       │
│ ┌────────────────────────┐                          │
│ │ 价格_高:                │                          │
│ │ [输入框]                │ 例如：199元              │
│ └────────────────────────┘                          │
│ ┌────────────────────────┐                          │
│ │ 价格_低:                │                          │
│ │ [输入框]                │ 例如：49元               │
│ └────────────────────────┘                          │
│ ... (其他变量)                                        │
│                                                       │
│ ⚠️ 未填写的变量将保留{变量名}格式，可后续手动编辑         │
│                                                       │
│ ┌────────────┐  ┌────────┐                          │
│ │ 生成A/B脚本 │  │ 取消   │                          │
│ └────────────┘  └────────┘                          │
└───────────────────────────────────────────────────────┘
```

#### 3.2 交互逻辑

**表单验证**:
- topicId: 必填（下拉选择当前项目的选题列表）
- 变量：可选（允许部分填写）

**提交流程**:
1. 调用 `POST /api/templates/:id/apply`
2. saveToDatabase: true
3. 成功后显示toast："已生成A/B两个版本脚本"
4. 跳转到Scripts页面并高亮新生成的脚本

### 4. Scripts页面集成

**新增功能**: "从模板创建"按钮

#### 4.1 页面修改

```
┌─────────────────────────────────────────────────────────┐
│ 超级洞察 > Scripts                                      │
│ [从模板创建] [批量生成] ...                              │
├─────────────────────────────────────────────────────────┤
│ ScriptCard ...                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.2 "从模板创建"流程

1. 点击按钮，打开TemplateSelectModal（简化版Templates页面）
2. 用户选择模板
3. 打开VariableFormModal
4. 提交后生成脚本

#### 4.3 ScriptCard新增操作

**"保存为模板"按钮**:
- 位置：ScriptCard的操作菜单（右上角三点）
- 功能：打开SaveAsTemplateModal

**SaveAsTemplateModal**:
```
┌───────────────────────────────────────────────────────┐
│ 保存为模板                                   [×]        │
├───────────────────────────────────────────────────────┤
│ ┌────────────────────────┐                          │
│ │ 模板名称:               │ *必填                    │
│ │ [输入框]                │                          │
│ └────────────────────────┘                          │
│ ┌────────────────────────┐                          │
│ │ 模板描述:               │                          │
│ │ [文本域]                │                          │
│ └────────────────────────┘                          │
│ ┌────────────────────────┐                          │
│ │ 分类: [下拉选择]         │                          │
│ └────────────────────────┘                          │
│ ┌────────────────────────┐                          │
│ │ 平台: [下拉选择]         │                          │
│ └────────────────────────┘                          │
│ ┌────────────────────────┐                          │
│ │ 标签: [标签输入]         │                          │
│ └────────────────────────┘                          │
│                                                       │
│ ⚠️ 脚本中的具体内容会保留，你可以后续编辑添加变量占位符   │
│                                                       │
│ ┌────────┐  ┌────────┐                              │
│ │ 保存   │  │ 取消   │                              │
│ └────────┘  └────────┘                              │
└───────────────────────────────────────────────────────┘
```

---

## 🛠️ 技术实现

### 前端架构

**页面结构**:
```
src/
├── pages/
│   └── Templates.tsx              # 模板库主页面（新增）
├── components/
│   └── templates/                 # 模板相关组件（新增）
│       ├── TemplateCard.tsx       # 模板卡片
│       ├── TemplateDetailModal.tsx # 模板详情弹窗
│       ├── VariableFormModal.tsx  # 变量填写弹窗
│       ├── TemplateSelectModal.tsx # 模板选择弹窗（简化版）
│       ├── SaveAsTemplateModal.tsx # 保存为模板弹窗
│       └── TemplateStats.tsx      # 统计信息组件
├── store/
│   └── template.store.ts          # 模板状态管理（新增）
└── api/
    └── template.api.ts            # 模板API封装（新增）
```

### 1. template.store.ts

**Zustand store设计**:
```typescript
interface TemplateStore {
  // 状态
  templates: ScriptTemplate[]
  loading: boolean
  filters: {
    category?: string
    platform?: string
    projectId?: string | 'global'
    search?: string
  }
  pagination: {
    total: number
    limit: number
    offset: number
    hasMore: boolean
  }
  
  // 操作
  fetchTemplates: () => Promise<void>
  fetchTemplateById: (id: string) => Promise<ScriptTemplate>
  createTemplate: (data: CreateTemplateData) => Promise<ScriptTemplate>
  applyTemplate: (id: string, data: ApplyTemplateData) => Promise<Script[]>
  saveScriptAsTemplate: (scriptId: string, data: SaveAsTemplateData) => Promise<ScriptTemplate>
  setFilters: (filters: Partial<TemplateFilters>) => void
  setSearch: (search: string) => void
}
```

### 2. template.api.ts

**API封装**:
```typescript
export const templateApi = {
  // 获取模板列表
  getTemplates: (params?: TemplateQueryParams) => 
    axios.get('/api/templates', { params }),
  
  // 获取模板详情
  getTemplateById: (id: string) => 
    axios.get(`/api/templates/${id}`),
  
  // 获取统计信息
  getStats: (projectId?: string) => 
    axios.get('/api/templates/stats', { params: { project_id: projectId } }),
  
  // 创建模板
  createTemplate: (data: CreateTemplateData) => 
    axios.post('/api/templates', data),
  
  // 应用模板生成脚本
  applyTemplate: (id: string, data: ApplyTemplateData) => 
    axios.post(`/api/templates/${id}/apply`, data),
  
  // 脚本保存为模板
  saveScriptAsTemplate: (scriptId: string, data: SaveAsTemplateData) => 
    axios.post(`/api/templates/scripts/${scriptId}/save-as-template`, data)
}
```

### 3. 组件实现要点

**TemplateCard**:
- 复用TopicCard的样式和交互（hover效果）
- 使用Badge组件显示分类和平台
- 使用lucide-react图标

**VariableFormModal**:
- 动态生成表单字段（基于template.variables）
- 使用Input组件（复用现有）
- 表单验证（topicId必填）
- 提交时显示LoadingSpinner

**SaveAsTemplateModal**:
- 使用Modal组件（复用现有）
- TagInput组件用于标签输入
- 下拉选择使用Select组件

---

## 📊 开发计划

### Phase 1: 基础设施（45分钟）

**Task 1.1: Store和API封装**
- 创建template.store.ts
- 创建template.api.ts
- 集成到现有架构
- **耗时**: 30分钟

**Task 1.2: 路由配置**
- 添加/templates路由
- 更新Sidebar导航菜单
- 权限控制（authMiddleware）
- **耗时**: 15分钟

### Phase 2: Templates页面（90分钟）

**Task 2.1: TemplateCard组件**
- 卡片布局和样式
- Badge显示（分类/平台/官方）
- 悬停效果
- **耗时**: 30分钟

**Task 2.2: Templates主页面**
- 页面布局（搜索/筛选/列表）
- 分类统计卡片
- 分页组件
- **耗时**: 40分钟

**Task 2.3: 搜索和筛选**
- 搜索输入框（debounce）
- 分类/平台下拉选择
- 实时更新列表
- **耗时**: 20分钟

### Phase 3: 模板应用流程（60分钟）

**Task 3.1: TemplateDetailModal**
- 模板详情展示
- 分段结构可视化
- 变量列表显示
- **耗时**: 25分钟

**Task 3.2: VariableFormModal**
- 动态表单生成
- 选题下拉选择
- 表单验证和提交
- **耗时**: 35分钟

### Phase 4: Scripts页面集成（60分钟）

**Task 4.1: "从模板创建"功能**
- TemplateSelectModal组件
- 集成到Scripts页面
- 跳转和高亮逻辑
- **耗时**: 30分钟

**Task 4.2: "保存为模板"功能**
- SaveAsTemplateModal组件
- ScriptCard操作菜单更新
- 提交和反馈
- **耗时**: 30分钟

### Phase 5: 测试与优化（45分钟）

**Task 5.1: 功能测试**
- 完整流程测试（模板选择→变量填写→脚本生成）
- 边界条件测试（空变量、长文本等）
- 错误处理测试
- **耗时**: 25分钟

**Task 5.2: UI优化**
- 响应式布局验证
- 加载状态优化
- 过渡动画调整
- **耗时**: 20分钟

### Phase 6: 文档归档（30分钟）

**Task 6.1: 更新CHANGELOG**
- 添加v2.14.0条目
- 功能列表和截图
- **耗时**: 15分钟

**Task 6.2: 工作总结**
- 创建WORK-SUMMARY-v2.14.0-Complete.md
- 记录开发过程和问题
- **耗时**: 15分钟

---

## ⏱️ 时间估算

| 阶段 | 任务 | 预估耗时 |
|------|------|----------|
| Phase 1 | 基础设施 | 45分钟 |
| Phase 2 | Templates页面 | 90分钟 |
| Phase 3 | 模板应用流程 | 60分钟 |
| Phase 4 | Scripts集成 | 60分钟 |
| Phase 5 | 测试优化 | 45分钟 |
| Phase 6 | 文档归档 | 30分钟 |
| **总计** | | **5.5小时** |

**缓冲时间**: 0.5-1小时（处理意外问题）  
**总预算**: 6小时

---

## 📈 ROI分析

### 开发成本
- 时间: 6小时
- 复杂度: 中等（前端UI，集成现有后端）

### 业务价值

**立即可用**:
- ✅ 用户可通过UI使用模板功能
- ✅ 3个预置模板立即产生价值
- ✅ v2.13.0的6小时投入开始产生回报

**用户体验提升**:
- 📈 脚本创建速度从30分钟降到5分钟（83%提升）
- 📈 减少80%重复工作
- 📈 可视化操作降低学习成本

**数据价值**:
- 📊 usage_count追踪热门模板
- 📊 用户创建的自定义模板积累
- 📊 为后续优化提供数据支持

### ROI计算

**假设**:
- 每周50个脚本
- 模板节省25分钟/脚本
- 50%脚本使用模板（25个）

**时间节省**:
- 每周: 25脚本 × 25分钟 = 625分钟 ≈ 10.4小时
- 每月: 10.4小时 × 4周 = 41.6小时
- 年度: 41.6小时 × 12月 = 499.2小时

**ROI**: (6小时 v2.13.0 + 6小时 v2.14.0) → 499.2小时年度回报 = **42倍ROI**

---

## 🎯 成功标准

### 功能完整性
- ✅ Templates页面正常显示所有模板
- ✅ 搜索和筛选功能正常工作
- ✅ 模板应用流程完整（选择→填写→生成）
- ✅ 脚本保存为模板功能正常
- ✅ Scripts页面集成无缝

### 用户体验
- ✅ 界面美观，与现有设计系统一致
- ✅ 交互流畅，无明显卡顿
- ✅ 错误提示友好，引导用户操作
- ✅ 响应式布局适配移动端

### 性能
- ✅ 模板列表加载<1秒
- ✅ 变量表单提交<2秒
- ✅ 脚本生成完成<3秒

### 测试覆盖
- ✅ 核心流程端到端测试
- ✅ 边界条件测试
- ✅ 错误处理测试

---

## 🚨 风险评估

### 技术风险

**风险1: 动态表单复杂度**
- **描述**: 根据template.variables动态生成表单，可能有14个字段
- **影响**: 中
- **缓解**: 使用成熟的表单库（react-hook-form），分组显示变量

**风险2: Scripts页面集成**
- **描述**: Scripts页面可能有其他正在进行的修改
- **影响**: 低
- **缓解**: 最小化修改，仅添加按钮和调用modal

### 时间风险

**风险3: 时间超预期**
- **描述**: 实际开发可能超过6小时
- **影响**: 中
- **缓解**: 分阶段交付，Phase 1-3先完成核心功能，Phase 4可延后

### 产品风险

**风险4: 用户不使用模板功能**
- **描述**: UI开发完成后用户使用率低
- **影响**: 高（ROI无法实现）
- **缓解**: 
  - 在Scripts页面显著位置放置"从模板创建"按钮
  - 新用户引导（首次使用时的提示）
  - 预置3个高质量模板降低门槛

---

## 📋 待办事项清单

### 开发前准备
- [ ] 审批产品规划文档
- [ ] 确认版本号（v2.13.1 vs v2.14.0）
- [ ] 确认UI设计规范（复用现有组件）

### 开发阶段
- [ ] Phase 1: 基础设施
- [ ] Phase 2: Templates页面
- [ ] Phase 3: 模板应用流程
- [ ] Phase 4: Scripts集成
- [ ] Phase 5: 测试优化
- [ ] Phase 6: 文档归档

### 发布前检查
- [ ] 端到端测试通过
- [ ] 性能指标达标
- [ ] CHANGELOG更新
- [ ] 工作总结完成

---

## 🔮 未来扩展（Phase 3）

**不在v2.14.0范围内，但值得规划**:

### 模板编辑器
- 可视化编辑segments
- 拖拽调整分段顺序
- 实时预览

### 模板市场
- 用户分享模板
- 模板评分和评论
- 热门模板推荐

### 智能推荐
- 根据项目类型推荐模板
- 根据历史使用推荐
- 基于产品信息匹配模板

### 协作功能
- 团队共享模板库
- 模板权限管理
- 模板版本历史

---

## 💡 决策建议

**推荐**: 立即启动 v2.14.0 开发

**理由**:
1. **最高ROI**: 让v2.13.0的6小时投入产生实际价值
2. **用户急需**: 后端已完成，用户等待UI使用
3. **技术可行**: 前端工作量适中，风险可控
4. **业务价值**: 立即提升脚本创作效率

**替代方案**: 如果资源有限，可以分阶段交付
- v2.14.0 Phase 1: Templates页面 + 模板应用（4小时）
- v2.14.1 Phase 2: Scripts集成（2小时）

---

**规划完成时间**: 2026-04-12  
**规划人员**: Claude (Autonomous Agent)  
**状态**: 📋 待审批  
**建议**: ✅ 立即启动开发
