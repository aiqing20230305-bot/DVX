# v2.16.0 手动测试日志 (TC-7)

**测试日期**: 2026-04-12  
**测试人员**: 开发团队  
**测试版本**: v2.16.0  
**测试功能**: 脚本版本历史管理

---

## 测试环境

- **前端**: http://localhost:5179
- **后端**: http://localhost:3001
- **浏览器**: Chrome 最新版
- **数据库**: SQLite (data.db)
- **操作系统**: macOS

---

## 测试执行指南

### 前置条件

1. ✅ 开发服务器已启动
   - 后端: http://localhost:3001 (PID 49476)
   - 前端: http://localhost:5179
   - 数据库迁移: Migration 11 已执行

2. ✅ 代码构建成功
   - 无TypeScript编译错误
   - 所有依赖已安装

3. 需要准备的测试数据
   - [ ] 至少1个项目
   - [ ] 至少1个选题
   - [ ] 至少1个脚本（用于测试版本历史）

---

## 测试用例 TC-7: 脚本版本历史功能

### TC-7.1: 创建历史记录

**目标**: 验证保存脚本时自动创建历史记录

**步骤**:
1. 登录系统
2. 进入"脚本创作"页面
3. 选择一个现有脚本或创建新脚本
4. 在ScriptEditor中修改内容：
   - 修改某个分镜的文案
   - 或添加新的分镜
   - 或删除某个分镜
5. 点击保存按钮（或按Cmd+S）
6. 观察是否有保存成功提示

**预期结果**:
- [ ] 保存成功后显示Toast提示
- [ ] 控制台无JavaScript错误
- [ ] 后端日志显示历史记录创建（可选验证）
- [ ] 数据库script_history表新增一条记录（可选验证）

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**:
```
待填写
```

---

### TC-7.2: 查看历史列表

**目标**: 验证版本历史Modal正确显示历史列表

**步骤**:
1. 在已有历史记录的脚本编辑器中
2. 按快捷键 Cmd+H (Mac) 或 Ctrl+H (Windows)
   - 或点击编辑器右上角菜单 → "版本历史"
3. 观察ScriptHistoryModal是否正确打开
4. 检查左侧版本列表：
   - 是否按版本号降序排列（最新版本在最上）
   - 是否显示"当前"标签
   - 时间显示是否正确（相对时间）
   - 字数统计是否正确
   - 字数变化是否正确显示（+/-）

**预期结果**:
- [ ] Modal正确打开，无报错
- [ ] 版本列表按降序排列
- [ ] 最新版本显示"当前"蓝色标签
- [ ] 时间显示格式正确（刚刚/X分钟前/X小时前/X天前）
- [ ] 字数统计显示正确
- [ ] 字数变化用颜色区分（绿色+，红色-，灰色0）
- [ ] 可以滚动查看更多历史版本

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**:
```
待填写
```

---

### TC-7.3: 查看版本详情

**目标**: 验证点击版本后正确加载和显示详情

**步骤**:
1. 在ScriptHistoryModal中
2. 点击左侧某个历史版本卡片
3. 观察右侧是否显示该版本的完整内容
4. 检查显示内容：
   - 版本号
   - 创建时间（绝对时间）
   - 分镜列表
   - 每个分镜的类型、内容、镜头、时长

**预期结果**:
- [ ] 点击版本后右侧加载详情（如果之前未加载）
- [ ] 版本号和时间正确显示
- [ ] 分镜列表完整显示
- [ ] 每个分镜的4个字段都正确显示
- [ ] 无内容截断或格式错误
- [ ] 切换不同版本时内容正确更新

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**:
```
待填写
```

---

### TC-7.4: 回退到历史版本

**目标**: 验证版本回退功能的正确性和安全性

**步骤**:
1. 在ScriptHistoryModal中选择一个历史版本（非当前版本）
2. 点击"回退到此版本"按钮
3. 观察是否弹出确认对话框
4. 阅读确认对话框内容：
   - 显示要回退的版本号
   - 警告信息："当前内容将被替换"
5. 点击"确认回退"
6. 等待回退操作完成
7. 观察：
   - Toast提示
   - Modal是否关闭
   - 脚本编辑器内容是否更新
8. 再次打开版本历史，验证：
   - 是否新增一条历史记录（回退点）
   - 版本号是否递增

**预期结果**:
- [ ] 点击"回退到此版本"后显示确认对话框
- [ ] 确认对话框显示正确的版本号
- [ ] 点击"确认回退"后执行回退
- [ ] 显示Toast提示"版本回退成功，已回退到版本 vX"
- [ ] Modal自动关闭
- [ ] 脚本编辑器内容更新为历史版本内容
- [ ] 新增一条历史记录标记回退点
- [ ] 版本号正确递增
- [ ] 可以再次回退到回退前的版本（双向可逆）

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**:
```
待填写
```

---

### TC-7.5: 边界场景测试

#### 场景1: 空历史列表（新脚本）

**步骤**:
1. 创建一个全新的脚本
2. 在未保存任何修改的情况下按Cmd+H
3. 观察ScriptHistoryModal显示

**预期结果**:
- [ ] Modal正常打开
- [ ] 左侧显示"暂无历史记录"空状态
- [ ] 显示History图标和提示文字
- [ ] 右侧显示"选择一个版本查看详情"提示

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

---

#### 场景2: 权限不足（Viewer角色）

**步骤**:
1. 使用Viewer角色账号登录（如果有）
2. 进入某个项目的脚本页面
3. 尝试打开版本历史（应该可以查看）
4. 尝试回退版本（应该被阻止）

**预期结果**:
- [ ] Viewer可以查看版本历史
- [ ] Viewer不能回退版本（按钮禁用或API返回403）
- [ ] 如果API返回403，显示Toast错误提示

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**: 如果测试环境无Viewer角色账号，可跳过此测试

---

#### 场景3: 网络失败模拟

**步骤**:
1. 打开Chrome DevTools
2. 切换到Network标签
3. 启用"Offline"模式模拟断网
4. 尝试：
   - 打开版本历史（应该失败）
   - 回退版本（应该失败）
5. 观察错误处理

**预期结果**:
- [ ] 网络失败时显示Toast错误提示
- [ ] 错误提示信息友好（如"加载历史记录失败"、"回退失败，请重试"）
- [ ] Modal保持打开状态，允许重试
- [ ] 控制台有明确的错误日志

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

---

#### 场景4: 历史创建静默失败

**步骤**:
1. 打开Chrome DevTools Console
2. 修改并保存脚本
3. 观察控制台是否有"Failed to create history record"警告
4. 验证主流程（保存脚本）不受影响

**预期结果**:
- [ ] 即使历史创建失败，保存脚本仍然成功
- [ ] 控制台有警告日志但不影响用户操作
- [ ] 用户无感知（不显示错误Toast）

**实际结果**:
```
待填写
```

**状态**: [ ] 通过 [ ] 失败 [ ] 阻塞

**备注**: 此场景较难模拟，可通过修改代码临时制造错误来测试

---

## 数据库验证（可选）

### 验证script_history表结构

```sql
-- 查看表结构
PRAGMA table_info(script_history);

-- 预期字段:
-- id TEXT PRIMARY KEY
-- script_id TEXT NOT NULL
-- version INTEGER NOT NULL
-- segments TEXT NOT NULL
-- full_text TEXT NOT NULL
-- word_count INTEGER NOT NULL
-- created_at INTEGER NOT NULL
```

**状态**: [ ] 已验证 [ ] 跳过

---

### 验证历史记录数据

```sql
-- 查看某个脚本的历史记录
SELECT 
  id,
  version,
  word_count,
  datetime(created_at/1000, 'unixepoch') as created_time
FROM script_history
WHERE script_id = 'YOUR_SCRIPT_ID'
ORDER BY version DESC;
```

**预期结果**:
- version字段连续递增（1, 2, 3...）
- created_at时间戳合理
- word_count符合实际字数

**状态**: [ ] 已验证 [ ] 跳过

---

### 验证级联删除

```sql
-- 删除一个脚本
DELETE FROM scripts WHERE id = 'YOUR_SCRIPT_ID';

-- 验证历史记录自动删除
SELECT COUNT(*) FROM script_history WHERE script_id = 'YOUR_SCRIPT_ID';
-- 预期: 0
```

**状态**: [ ] 已验证 [ ] 跳过

---

## 性能测试（可选）

### 历史列表加载性能

**测试条件**: 10个历史版本

**测量指标**:
- [ ] API响应时间: < 200ms
- [ ] 首次渲染时间: < 100ms
- [ ] 无明显卡顿

**实际结果**:
```
待填写
```

---

### 版本详情加载性能

**测试条件**: 单个版本详情（包含10个分镜）

**测量指标**:
- [ ] API响应时间: < 100ms
- [ ] 渲染时间: < 50ms

**实际结果**:
```
待填写
```

---

### 回退操作性能

**测试条件**: 回退到历史版本

**测量指标**:
- [ ] 完整操作时间: < 300ms
- [ ] 包括: API请求 + 数据更新 + UI刷新

**实际结果**:
```
待填写
```

---

## UI/UX检查清单

### 视觉一致性
- [ ] Modal设计符合项目整体风格
- [ ] 颜色使用符合设计系统（主色 #5E6AD2）
- [ ] 字体和字号符合规范
- [ ] 间距符合8px基准
- [ ] 深色主题显示正常

### 交互反馈
- [ ] 按钮hover/active状态正常
- [ ] 加载状态有视觉反馈（Loading Spinner）
- [ ] Toast提示位置和时长合理
- [ ] 确认对话框按钮布局清晰

### 可访问性
- [ ] 键盘导航可用（Tab顺序合理）
- [ ] Escape键可关闭Modal
- [ ] 焦点状态清晰可见
- [ ] 颜色对比度符合WCAG AA标准

### 响应式设计
- [ ] 在1920×1080分辨率下显示正常
- [ ] 在1366×768分辨率下显示正常
- [ ] Modal宽度和高度自适应
- [ ] 滚动条显示正常

---

## 浏览器兼容性测试（可选）

### Chrome
- [ ] 最新版本测试通过
- 版本号: _______

### Safari
- [ ] 最新版本测试通过
- 版本号: _______

### Firefox
- [ ] 最新版本测试通过
- 版本号: _______

---

## 缺陷记录

### Bug #1
**严重程度**: [ ] P0-Critical [ ] P1-High [ ] P2-Medium [ ] P3-Low

**标题**:
```
待填写
```

**描述**:
```
待填写
```

**复现步骤**:
```
1. 
2. 
3. 
```

**预期结果**:
```
待填写
```

**实际结果**:
```
待填写
```

**环境**:
- 浏览器: 
- 操作系统: 
- 版本: 

**截图/日志**:
```
待填写
```

---

## 测试总结

### 执行统计

- **总测试用例数**: 9个（TC-7.1 ~ TC-7.5四个场景）
- **通过数**: ___
- **失败数**: ___
- **阻塞数**: ___
- **跳过数**: ___
- **通过率**: ___%

### 主要发现

**功能正常**:
```
待填写
```

**功能异常**:
```
待填写
```

**建议改进**:
```
待填写
```

### 测试结论

[ ] ✅ 测试通过 - 功能符合预期，可以发布  
[ ] ⚠️ 测试通过（有建议）- 功能正常，但有优化空间  
[ ] ❌ 测试失败 - 存在严重问题，需要修复

### 签字确认

**测试人员**: _____________  
**日期**: _____________  
**备注**:
```
待填写
```

---

## 附录

### 测试数据

**测试项目**:
- 项目ID: _______
- 项目名称: _______

**测试脚本**:
- 脚本ID: _______
- 脚本标题: _______
- 初始版本数: _______

### 参考文档

- WORK-SUMMARY-v2.16.0.md
- CHANGELOG.md (v2.16.0)
- /src/components/scripts/ScriptHistoryModal.tsx
- /server/routes/script.route.ts

### 测试工具

- Chrome DevTools
- Network Throttling (模拟慢网络)
- Offline Mode (模拟断网)
- SQLite Browser (数据库验证)

---

**文档版本**: 1.0  
**最后更新**: 2026-04-12
