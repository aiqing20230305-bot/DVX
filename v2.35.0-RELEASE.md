# v2.35.0 Release Notes

**发布日期**: 2026-04-15  
**类型**: Feature + Bugfix Release  
**状态**: ✅ Stable - Ready for Production

---

## 🎯 本次更新概览

本次更新主要解决了UI主题、权限系统、选题状态同步等关键问题，并新增了Word文档上传支持，提升了系统的稳定性和易用性。

---

## ✨ 新功能

### 1. Word文档上传支持
- **文件格式**: 支持 `.doc` 和 `.docx` 格式
- **解析能力**: 使用 `mammoth` 库提取文本内容和元数据（字数、段落数）
- **应用场景**: 用户可上传Word格式的产品资料、市场数据等
- **相关文件**:
  - `server/services/parser/word-parser.ts` (新增)
  - `server/middleware/upload.middleware.ts`
  - `server/routes/upload.route.ts`
  - `src/components/workbench/DropZone.tsx`
  - `package.json` (添加 mammoth 依赖)

---

## 🐛 问题修复

### 1. UI主题恢复为白色（亮色）
- **问题**: 默认主题被设置为深色，用户体验不佳
- **修复**: 将 `ui.store.ts` 默认主题改为 `'light'`
- **相关文件**: `src/store/ui.store.ts`

### 2. 审批管理功能移除
- **原因**: 该功能暂不需要，简化产品流程
- **修复**: 移除了审批管理相关的路由和菜单项
- **相关文件**: 
  - `src/App.tsx` (移除 `/approvals` 路由)
  - `src/components/layout/Sidebar.tsx` (移除"审批管理"菜单项)

### 3. Modal输入框焦点跳转问题 ⭐ 重要
- **问题**: 用户在模态框输入框打字时，每输入一个字符焦点就跳到关闭按钮
- **根本原因**: Modal组件的 `useEffect` 依赖数组包含了 `onClose`，导致每次父组件状态变化时都重新执行焦点逻辑
- **修复**: 
  - 移除 `useEffect` 中的 `onClose` 依赖
  - 修改焦点优先级：优先聚焦 `input/textarea`，而非 `button`
  - 添加全局 CSS 焦点样式以增强可见性
- **相关文件**: 
  - `src/components/shared/Modal.tsx`
  - `src/styles/globals.css`

### 4. 选题选择状态在页面切换时丢失 ⭐ 重要
- **问题**: 用户在"选题策划"页面勾选选题后，切换到"脚本创作"页面显示"没有已选择的选题"
- **根本原因**: 
  - 用户勾选只更新前端 `selectedIds` (临时状态)
  - 切换页面时 `setTopics()` 根据数据库 `selected` 字段重置了 `selectedIds`
  - 导致前端勾选状态丢失
- **修复**: 
  - 修改 `topic.store.ts` 的 `setTopics()` 函数，保留现有 `selectedIds`
  - 修改 `Scripts.tsx` 的过滤逻辑，只使用 `selectedIds`（不依赖数据库 `selected` 字段）
  - 修改 `batchUpdateSelected()` 函数，同步更新 `selectedIds` 和 `topics`
- **用户体验改进**: 现在用户勾选后无需点击"标记为已选择"按钮，即可在脚本页面看到选中的选题
- **相关文件**: 
  - `src/store/topic.store.ts`
  - `src/pages/Scripts.tsx`

### 5. 开发环境权限检查绕过不完整
- **问题**: 多个路由使用了手动权限检查（`projectMemberRepo.hasRole()`），没有绕过开发环境，导致403错误
- **影响范围**: 批量选择/删除选题、修改优先级、单个选题/洞察/脚本编辑等操作
- **修复**: 
  - 在 `permission.middleware.ts` 新增 `checkPermission()` 辅助函数
  - 在开发环境下自动返回 `true`
  - 更新 3 个路由文件使用统一的权限检查函数
- **相关文件**: 
  - `server/middleware/permission.middleware.ts` (新增 `checkPermission` 函数)
  - `server/routes/topic.route.ts` (9处修复)
  - `server/routes/insight.route.ts` (2处修复)
  - `server/routes/script.route.ts` (3处修复)

### 6. UI配色统一为Linear风格
- **问题**: 部分组件使用了不一致的配色（橙色、琥珀色等）
- **修复**: 统一使用 Linear 紫色系（`#5E6AD2`, `#7B85DB`, `#4A55B8`）
- **影响组件**:
  - `src/components/scripts/ScriptEditor.tsx` (脚本分段配色)
  - `src/components/timeline/ProjectTimeline.tsx` (时间轴动作配色)

---

## 📊 技术细节

### 依赖变更
```json
{
  "mammoth": "^1.8.0"  // 新增：用于Word文档解析
}
```

### 数据库变更
- 无数据库结构变更

### API变更
- 无破坏性API变更
- 新增：Word文档解析支持（通过现有 `/api/upload` 端点）

---

## 🧪 测试覆盖

### 手动测试通过 ✅
1. Word文档上传和解析
2. 选题勾选 → 切换页面 → 状态保持
3. Modal输入框正常输入（无焦点跳转）
4. 批量选题操作（选择/删除/优先级调整）
5. 开发环境权限绕过

### 待补充自动化测试
- [ ] Word文档解析单元测试
- [ ] 选题状态同步集成测试
- [ ] Modal焦点行为E2E测试

---

## 📝 文件变更统计

```
17 files changed, 122 insertions(+), 94 deletions(-)

核心变更:
- server/middleware/permission.middleware.ts  (+20)
- server/services/parser/word-parser.ts       (新增)
- src/store/topic.store.ts                    (+21)
- src/components/shared/Modal.tsx             (+19/-13)
```

---

## 🚀 部署步骤

### 1. 安装依赖
```bash
npm install
```

### 2. 重启服务
```bash
npm run dev  # 开发环境
# 或
npm run build && npm start  # 生产环境
```

### 3. 验证功能
- [ ] 上传Word文档
- [ ] 选题勾选后切换页面
- [ ] 批量操作选题
- [ ] Modal输入框正常输入

---

## ⚠️ 注意事项

1. **开发环境权限**: 本地开发时所有权限检查自动绕过（`NODE_ENV !== 'production'`）
2. **选题状态**: 用户勾选的选题是会话级状态，刷新页面后需重新勾选（除非点击"标记为已选择"保存到数据库）
3. **Word文档**: 目前仅支持文本提取，不支持表格、图片等复杂格式

---

## 🔄 版本历史

- **v2.34.0**: 质量评分系统
- **v2.33.0**: 多格式导出系统
- **v2.35.0**: Word上传 + 状态同步修复 (本次发布)

---

## 👥 贡献者

- **开发**: Claude (AI Assistant)
- **测试**: 张经纬 (Product Owner)

---

## 📞 问题反馈

如遇到问题，请联系开发团队或在项目仓库提交 Issue。
