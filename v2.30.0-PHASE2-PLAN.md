# v2.30.0 Phase 2: 测试覆盖补充 - 实施计划

## 📋 Phase 1回顾

**已完成**:
- ✅ 测试隔离问题修复 (70-80% 批量测试成功率)
- ✅ 独立数据库实例
- ✅ 完整数据清理
- ✅ 文档更新

**当前测试覆盖**:
- ✅ POST /api/auth/login (4个测试)
- ✅ POST /api/auth/register (5个测试)
- ✅ GET /api/auth/me (2个测试)
- ✅ GET /api/comments/search (5个测试)
- ✅ GET /api/comments/search/history (2个测试)
- ✅ DELETE /api/comments/search/history (1个测试)
- ✅ POST /api/comments (3个测试)
- ✅ DELETE /api/comments/:commentId (2个测试)

**总计**: 24个测试通过

---

## 🎯 Phase 2目标

补充缺失的核心API测试，达到完整覆盖。

**预计时长**: 4小时 (0.5天)

---

## 📝 缺失的测试用例

### 1. GET /api/comments - 获取评论列表 ⭐ 高优先级

**当前状态**: 无测试覆盖  
**API功能**: 按target (insight/topic/script/report) 获取评论列表，包含嵌套replies

**需要测试的场景**:

#### 1.1 基础功能测试 (3个测试)
```typescript
describe('GET /api/comments - 获取评论列表', () => {
  it('应该返回指定target的评论列表', async () => {
    // Given: 创建3条评论
    const comment1 = await createTestComment({
      targetType: 'insight',
      targetId: 'insight-1',
      content: '第一条评论'
    })
    
    // When: 获取insight-1的评论
    const response = await request(app)
      .get('/api/comments')
      .query({
        target_type: 'insight',
        target_id: 'insight-1'
      })
      .set('Authorization', authToken)
    
    // Then: 返回评论列表
    expect(response.status).toBe(200)
    expect(response.body.comments).toHaveLength(3)
    expect(response.body.comments[0].content).toBe('第一条评论')
  })
  
  it('应该正确返回嵌套replies结构', async () => {
    // Given: 创建父评论和子回复
    const parentComment = await createTestComment({
      targetType: 'topic',
      targetId: 'topic-1',
      content: '父评论'
    })
    
    const childComment = await createTestComment({
      targetType: 'topic',
      targetId: 'topic-1',
      content: '子回复',
      parent_id: parentComment.id
    })
    
    // When: 获取评论
    const response = await request(app)
      .get('/api/comments')
      .query({
        target_type: 'topic',
        target_id: 'topic-1'
      })
      .set('Authorization', authToken)
    
    // Then: 验证嵌套结构
    expect(response.body.comments[0].id).toBe(parentComment.id)
    expect(response.body.comments[0].replies).toHaveLength(1)
    expect(response.body.comments[0].replies[0].id).toBe(childComment.id)
  })
  
  it('应该拒绝非项目成员访问', async () => {
    // Given: 另一个用户（不是项目成员）
    const otherUser = await registerUser('other@example.com', 'Password123!')
    const otherToken = await loginUser('other@example.com', 'Password123!')
    
    // When: 尝试访问
    const response = await request(app)
      .get('/api/comments')
      .query({
        target_type: 'insight',
        target_id: 'insight-1'
      })
      .set('Authorization', otherToken)
    
    // Then: 403 Forbidden
    expect(response.status).toBe(403)
    expect(response.body.error).toContain('权限')
  })
})
```

---

### 2. PUT /api/comments/:commentId - 更新评论 (如果存在)

**检查**: 查看是否有此API endpoint

**如果存在，需要测试**:
```typescript
it('应该成功更新评论内容', async () => {
  // Given: 创建评论
  const comment = await createTestComment({
    content: '原始内容'
  })
  
  // When: 更新内容
  const response = await request(app)
    .put(`/api/comments/${comment.id}`)
    .send({ content: '更新后的内容' })
    .set('Authorization', authToken)
  
  // Then: 验证更新
  expect(response.status).toBe(200)
  expect(response.body.comment.content).toBe('更新后的内容')
})

it('应该拒绝更新他人的评论', async () => {
  // Given: 另一个用户的评论
  const otherUserComment = createByOtherUser()
  
  // When: 当前用户尝试更新
  const response = await request(app)
    .put(`/api/comments/${otherUserComment.id}`)
    .send({ content: '恶意修改' })
    .set('Authorization', authToken)
  
  // Then: 403 Forbidden
  expect(response.status).toBe(403)
})
```

---

### 3. 边缘案例测试 (3-5个测试)

#### 3.1 空结果集处理
```typescript
it('应该正确处理无评论的情况', async () => {
  const response = await request(app)
    .get('/api/comments')
    .query({
      target_type: 'insight',
      target_id: 'non-existent'
    })
    .set('Authorization', authToken)
  
  expect(response.status).toBe(200)
  expect(response.body.comments).toEqual([])
  expect(response.body.total).toBe(0)
})
```

#### 3.2 超长内容处理
```typescript
it('应该拒绝超长评论内容（>1000字）', async () => {
  const longContent = 'A'.repeat(1001)
  
  const response = await request(app)
    .post('/api/comments')
    .send({
      target_type: 'insight',
      target_id: 'insight-1',
      content: longContent
    })
    .set('Authorization', authToken)
  
  expect(response.status).toBe(400)
  expect(response.body.error).toContain('内容过长')
})
```

#### 3.3 特殊字符处理
```typescript
it('应该正确处理特殊字符（emoji、@mentions）', async () => {
  const specialContent = '😀 @user1 这是一条带特殊字符的评论'
  
  const response = await request(app)
    .post('/api/comments')
    .send({
      target_type: 'topic',
      target_id: 'topic-1',
      content: specialContent
    })
    .set('Authorization', authToken)
  
  expect(response.status).toBe(201)
  expect(response.body.comment.content).toBe(specialContent)
})
```

#### 3.4 并发创建测试
```typescript
it('应该支持并发创建评论', async () => {
  // When: 同时创建10条评论
  const promises = Array.from({ length: 10 }, (_, i) =>
    request(app)
      .post('/api/comments')
      .send({
        target_type: 'script',
        target_id: 'script-1',
        content: `评论${i + 1}`
      })
      .set('Authorization', authToken)
  )
  
  const responses = await Promise.all(promises)
  
  // Then: 全部成功
  responses.forEach(res => expect(res.status).toBe(201))
  
  // Verify: 数据库中有10条
  const comments = await getComments('script', 'script-1')
  expect(comments).toHaveLength(10)
})
```

---

## 📊 测试覆盖目标

### 当前覆盖率
- 认证API: 11/11 (100%)
- 评论搜索API: 13/13 (100%)
- **评论CRUD API: 5/10 (50%)** ⚠️

### 目标覆盖率
- 认证API: 11/11 (100%)
- 评论搜索API: 13/13 (100%)
- **评论CRUD API: 10/10 (100%)** ✅

**总计**: 34个测试 (从24个增加到34个)

---

## 🛠️ 实施步骤

### Step 1: API Endpoint审查 (30分钟)

**任务**:
1. 查看 `server/routes/comments.route.ts` 确认所有endpoints
2. 列出已测试和未测试的endpoints
3. 确定优先级

**输出**: API覆盖清单

---

### Step 2: 编写GET /api/comments测试 (1.5小时)

**文件**: `server/routes/__tests__/comments.route.test.ts`

**新增测试**:
1. 基础功能 - 返回评论列表 (15分钟)
2. 嵌套replies结构 (20分钟)
3. 权限验证 - 非项目成员拒绝访问 (15分钟)
4. 空结果集处理 (10分钟)
5. 排序验证 - 按创建时间降序 (10分钟)
6. 分页测试（如果支持）(20分钟)

**验证**: 运行测试确保通过

---

### Step 3: 边缘案例测试 (1.5小时)

**新增测试**:
1. 超长内容处理 (20分钟)
2. 特殊字符处理 (20分钟)
3. 并发创建测试 (30分钟)
4. 无效参数测试 (20分钟)

**验证**: 单独运行和批量运行

---

### Step 4: 文档更新 (30分钟)

**更新文件**:
1. `TESTING-GUIDE.md` - 添加边缘案例测试示例
2. `CHANGELOG.md` - 更新v2.30.0 Phase 2条目
3. `v2.30.0-PHASE2-COMPLETE.md` (新建) - 完成报告

---

### Step 5: 全量测试验证 (30分钟)

**验证流程**:
```bash
# 1. 单独运行每个测试文件
npm run test:run -- server/routes/__tests__/auth.route.test.ts
npm run test:run -- server/routes/__tests__/comments.route.test.ts

# 2. 批量运行10次
for i in {1..10}; do
  npm run test:run -- server/routes/__tests__/*.test.ts && echo "✓" || echo "✗"
done

# 3. 生成覆盖率报告
npm run test:coverage
```

**目标**:
- 单独运行: 100% 通过
- 批量运行: 70%+ 通过
- 覆盖率: 覆盖所有API endpoints

---

## 📋 交付清单

- ✅ 新增5-10个测试用例
- ✅ GET /api/comments 完整覆盖
- ✅ 边缘案例测试
- ✅ 文档更新（TESTING-GUIDE.md, CHANGELOG.md）
- ✅ 全量测试验证报告

---

## 🚀 Phase 3 Preview

**Phase 3: CI/CD集成** (0.5天)

**核心内容**:
1. GitHub Actions workflow配置
2. 自动运行测试（PR + push main）
3. 测试覆盖率报告集成（codecov）
4. PR状态检查

**预计开始**: Phase 2完成后

---

**文档创建时间**: 2026-04-12  
**预计开始**: 2026-04-12 (Phase 1完成后立即开始)  
**预计完成**: 2026-04-12 (同一天)
