# v2.27.0 产品规划文档

**规划日期**: 2026-04-12  
**版本号**: v2.27.0  
**规划类型**: 搜索性能优化 + 测试覆盖率提升  
**预计开发周期**: 4-5天

---

## 📋 背景分析

### v2.26.0 完成情况

**已完成功能** ✅:
1. **通知设置前端UI**（Phase 1，100%）
   - useNotificationSettings Hook（状态管理 + API封装）
   - NotificationSettingsPanel组件（11个开关 + 频率选择）
   - Settings页面集成（Tab导航）
   - Sidebar入口按钮
   - Migration 14（notification_settings表）

2. **搜索历史记录**（Phase 2，100%）
   - search_history表（Migration 15，复合索引优化）
   - 自动保存搜索历史（Upsert模式）
   - 历史下拉菜单（焦点显示、延迟关闭）
   - 一键清空历史
   - 搜索次数统计

3. **测试与文档归档**（Phase 3，100%）
   - package.json版本号更新（2.25.0 → 2.26.0）
   - v2.26.0-RELEASE-NOTES.md（完整发布说明）
   - WORK-SUMMARY-v2.26.0.md（工作总结）

**代码统计**:
- 新增代码：~1131行
- 新增表：2个（notification_settings, search_history）
- 新增API端点：4个
- 新增组件：3个（NotificationSettingsPanel, Settings, 搜索历史下拉）

**用户价值**:
- ✅ 通知控制权掌握（11个开关细粒度配置）
- ✅ 搜索效率提升（历史记录快速填充）
- ✅ 工作体验优化（减少通知打扰）

---

### 当前产品完整度评估

| 模块 | 功能完整度 | 核心功能 | 待优化点 |
|------|---------|---------|---------|
| **数据上传** | 95% | ✅ Excel/PDF解析 ✅ 批量上传 | 🔄 进度展示优化 |
| **洞察生成** | 95% | ✅ SSE流式生成 ✅ 批量生成 | 🔄 生成速度优化 |
| **选题策划** | 95% | ✅ 批量生成 ✅ 产品关联 | 🔄 智能推荐 |
| **脚本创作** | 95% | ✅ A/B版本 ✅ 模板系统 | 🔄 版本对比优化 |
| **报告导出** | 90% | ✅ PDF导出 ✅ 品牌化 | 🔄 PPT导出（可选） |
| **评论协作** | 95% | ✅ 评论系统 ✅ @提及 ✅ 搜索 | 🔄 搜索性能 |
| **通知系统** | 95% | ✅ 邮件通知 ✅ 站内通知 ✅ 设置 | 🔄 免打扰模式 |
| **权限管理** | 90% | ✅ 角色权限 ✅ 项目成员 | 🔄 细粒度权限 |
| **测试覆盖** | 40% | ⚠️ E2E测试 ⚠️ 单元测试 | 🔄 测试补充 |

**整体完整度**: **90%**

**核心短板识别**:
1. **测试覆盖率不足**（40%）- 缺少单元测试和集成测试
2. **搜索性能优化空间**（LIKE查询 → FTS5全文搜索）
3. **历史遗留功能**（3个pending任务）

---

## 🎯 v2.27.0 目标

### 核心目标

**主目标**: 提升搜索性能（50-70%），补充测试覆盖（40% → 70%），完成历史遗留功能

**用户价值**:
- 📈 搜索响应速度提升50-70%（FTS5性能优化）
- 📈 产品稳定性提升30%（测试覆盖率增加）
- 📈 专业度提升（PDF品牌化、完善测试）

**技术债务**:
- 🔧 补充单元测试（核心Repository和API）
- 🔧 补充集成测试（完整API流程）
- 🔧 完成历史遗留功能（PDF品牌化、前端UI测试）

---

## 📦 候选功能分析

### 1. FTS5全文搜索升级（优先级：高）⭐

**功能描述**: 将SQLite LIKE查询升级为FTS5全文搜索，提升大数据量下的性能

**当前痛点**:
- 评论搜索使用SQL LIKE查询
- 1000+条评论时搜索响应时间>500ms
- 无中文分词支持
- 无相关性排序

**技术实现**:

#### 1.1 数据库迁移（Migration 16）

创建FTS5虚拟表：
```sql
CREATE VIRTUAL TABLE comments_fts USING fts5(
  comment_id UNINDEXED,
  content,
  tokenize='unicode61 remove_diacritics 1'
);

-- Trigger: 同步INSERT
CREATE TRIGGER comments_fts_insert AFTER INSERT ON comments
BEGIN
  INSERT INTO comments_fts(comment_id, content) VALUES (NEW.id, NEW.content);
END;

-- Trigger: 同步UPDATE
CREATE TRIGGER comments_fts_update AFTER UPDATE ON comments
BEGIN
  UPDATE comments_fts SET content = NEW.content WHERE comment_id = OLD.id;
END;

-- Trigger: 同步DELETE
CREATE TRIGGER comments_fts_delete AFTER DELETE ON comments
BEGIN
  DELETE FROM comments_fts WHERE comment_id = OLD.id;
END;

-- 初始数据迁移
INSERT INTO comments_fts(comment_id, content)
SELECT id, content FROM comments;
```

#### 1.2 更新commentRepo.search()

```typescript
search(options: {
  keyword?: string
  // ...
}): { comments: CommentWithUser[], total: number } {
  const db = getDb()
  
  if (keyword && keyword.trim()) {
    // 使用FTS5 MATCH查询
    const searchStmt = db.prepare(`
      SELECT
        c.*,
        u.*,
        bm25(cf) as rank
      FROM comments_fts cf
      JOIN comments c ON cf.comment_id = c.id
      JOIN users u ON c.user_id = u.id
      WHERE cf.content MATCH ?
      ${whereClause}  -- 其他过滤条件
      ORDER BY rank  -- 按相关性排序
      LIMIT ? OFFSET ?
    `)
    
    // 使用FTS5查询语法（支持短语搜索、布尔运算）
    const ftsQuery = keyword.trim().split(' ').map(k => `"${k}"`).join(' OR ')
    const rows = searchStmt.all(ftsQuery, ...params, limit, offset)
    // ...
  } else {
    // 无关键词时，使用原有逻辑
    // ...
  }
}
```

#### 1.3 性能对比测试

**测试场景**:
- 1000条评论：LIKE vs FTS5 响应时间
- 5000条评论：LIKE vs FTS5 响应时间
- 10000条评论：LIKE vs FTS5 响应时间

**预期效果**:
- 1000条：~500ms → ~150ms（70%提升）
- 5000条：~2000ms → ~500ms（75%提升）
- 10000条：~4000ms → ~800ms（80%提升）

**开发时长**: 3-4小时

**用户价值**:
- 搜索响应速度提升50-70%
- 支持复杂查询（短语搜索、布尔运算）
- 相关性排序（bm25算法）

**技术风险**: 中
- 需要迁移现有数据
- 需要测试Trigger同步机制
- 需要验证FTS5查询语法

**注意事项**:
- FTS5 unicode61不支持中文分词（需要额外分词器）
- 当前方案适用于英文和简单中文搜索
- 未来可集成jieba分词（v2.28.0）

---

### 2. 搜索建议（自动补全）（优先级：中）

**功能描述**: 基于搜索历史和高频关键词，提供搜索建议

**当前痛点**:
- 用户需要完整输入关键词
- 无法发现高频搜索词
- 移动端输入效率低

**技术实现**:

#### 2.1 新增API端点

```typescript
// GET /api/comments/search/suggestions?q={prefix}&limit=10
router.get(
  '/api/comments/search/suggestions',
  authMiddleware,
  async (req: AuthRequest, res: Response) => {
    const userId = req.userId!
    const prefix = req.query.q as string
    const limit = parseInt(req.query.limit as string) || 10
    
    // 基于前缀匹配历史记录
    const suggestions = commentRepo.getSearchSuggestions(userId, prefix, limit)
    res.json({ suggestions })
  }
)
```

#### 2.2 更新commentRepo

```typescript
getSearchSuggestions(userId: string, prefix: string, limit: number = 10): Array<{
  keyword: string
  search_count: number
}> {
  const db = getDb()
  const stmt = db.prepare(`
    SELECT keyword, search_count
    FROM search_history
    WHERE user_id = ? AND keyword LIKE ?
    ORDER BY search_count DESC, last_search_at DESC
    LIMIT ?
  `)
  
  return stmt.all(userId, `${prefix}%`, limit) as any[]
}
```

#### 2.3 前端集成（CommentSearchModal）

```typescript
// 监听输入变化，获取建议
useEffect(() => {
  if (keyword.trim().length >= 2) {
    fetchSuggestions(keyword)
  } else {
    setSuggestions([])
  }
}, [keyword])

// 显示建议下拉
{suggestions.length > 0 && (
  <div className="absolute top-full ...">
    {suggestions.map((s) => (
      <button onClick={() => handleSuggestionClick(s.keyword)}>
        <Search />
        {s.keyword}
        <span>{s.search_count}次</span>
      </button>
    ))}
  </div>
)}
```

**开发时长**: 2小时

**用户价值**:
- 减少输入成本
- 发现高频关键词
- 移动端体验提升

**技术风险**: 低

**建议**: 可选，若Phase 1完成较快则开发

---

### 3. 单元测试补充（优先级：高）⭐

**功能描述**: 为核心Repository和Service补充单元测试，提升代码质量

**当前痛点**:
- 测试覆盖率仅40%
- 缺少Repository层单元测试
- 缺少Service层单元测试
- 代码重构风险高

**技术实现**:

#### 3.1 Repository层测试

**测试文件**: `server/db/repositories/*.test.ts`

**核心测试用例**:

```typescript
// commentRepo.test.ts
describe('commentRepo', () => {
  beforeEach(() => {
    // Setup test database
  })
  
  afterEach(() => {
    // Cleanup
  })
  
  describe('search()', () => {
    it('应该根据关键词搜索评论', () => {
      // Given: 创建测试评论
      // When: 搜索关键词"产品"
      // Then: 返回匹配的评论
    })
    
    it('应该按时间范围过滤', () => {
      // Given: 创建不同时间的评论
      // When: 搜索指定时间范围
      // Then: 返回范围内的评论
    })
    
    it('应该支持分页', () => {
      // Given: 创建30条评论
      // When: 请求limit=10&offset=10
      // Then: 返回第11-20条
    })
  })
  
  describe('saveSearchHistory()', () => {
    it('应该保存新的搜索历史', () => {
      // Given: 用户首次搜索"产品卖点"
      // When: 调用saveSearchHistory()
      // Then: 创建新记录，search_count=1
    })
    
    it('应该更新已有搜索历史', () => {
      // Given: 用户已搜索过"产品卖点"
      // When: 再次搜索
      // Then: search_count+1, last_search_at更新
    })
  })
})

// notificationSettingsRepo.test.ts
describe('notificationSettingsRepo', () => {
  describe('findOrCreateByUserId()', () => {
    it('应该返回已有设置', () => {
      // Given: 用户已有设置
      // When: 调用findOrCreateByUserId()
      // Then: 返回已有设置
    })
    
    it('应该创建默认设置', () => {
      // Given: 用户无设置
      // When: 调用findOrCreateByUserId()
      // Then: 创建默认设置，所有开关开启
    })
  })
  
  describe('update()', () => {
    it('应该更新部分字段', () => {
      // Given: 用户已有设置
      // When: 更新email_enabled=false
      // Then: 仅email_enabled更新，其他不变
    })
  })
})
```

**测试覆盖范围**:
- commentRepo: 10个测试用例
- notificationSettingsRepo: 6个测试用例
- userRepo: 8个测试用例
- projectRepo: 8个测试用例
- insightRepo: 8个测试用例
- topicRepo: 8个测试用例
- scriptRepo: 8个测试用例

**总计**: ~56个测试用例

**开发时长**: 6-8小时

**用户价值**:
- 代码质量提升
- 重构风险降低
- Bug减少30%

**技术风险**: 低

---

#### 3.2 API集成测试

**测试文件**: `server/routes/*.test.ts`

**核心测试场景**:

```typescript
// comments.route.test.ts
describe('POST /api/comments', () => {
  it('应该创建评论并返回201', async () => {
    // Given: 登录用户
    // When: POST /api/comments { target_type, target_id, content }
    // Then: 状态码201，返回评论对象
  })
  
  it('应该拒绝未登录用户', async () => {
    // Given: 未登录
    // When: POST /api/comments
    // Then: 状态码401
  })
  
  it('应该验证content长度', async () => {
    // Given: content长度>1000
    // When: POST /api/comments
    // Then: 状态码400，错误信息"内容不能超过1000字符"
  })
})

describe('GET /api/comments/search', () => {
  it('应该搜索评论', async () => {
    // Given: 已有评论
    // When: GET /api/comments/search?keyword=产品
    // Then: 返回匹配评论
  })
  
  it('应该保存搜索历史', async () => {
    // Given: 搜索关键词"产品"
    // When: GET /api/comments/search?keyword=产品
    // Then: search_history表中有记录
  })
})
```

**测试覆盖范围**:
- comments.route: 15个测试用例
- users.route: 10个测试用例
- projects.route: 12个测试用例
- insights.route: 10个测试用例
- topics.route: 10个测试用例
- scripts.route: 10个测试用例

**总计**: ~67个测试用例

**开发时长**: 8-10小时

**用户价值**:
- API稳定性提升
- 集成问题提前发现
- 回归测试自动化

**技术风险**: 中（需要Mock外部服务）

**建议**: Phase 2重点，若时间不足可分两期（v2.27.0 + v2.28.0）

---

### 4. PDF导出品牌化（优先级：中）

**功能描述**: 完成v2.21.0 Phase 2遗留功能，为PDF导出添加品牌元素

**当前状态**: 
- v2.21.0已完成基础PDF导出
- 缺少品牌logo、颜色主题、页眉页脚

**技术实现**:

#### 4.1 品牌元素设计

**Logo**:
- 位置：首页顶部居中或左上角
- 尺寸：200×60px
- 格式：PNG（透明背景）

**颜色主题**:
- 主色：#5E6AD2（Linear Purple）
- 辅色：#3A3A3A（深灰）
- 强调色：#10B981（成功绿）

**页眉页脚**:
- 页眉：项目名称 + Logo（左对齐）+ 生成日期（右对齐）
- 页脚：页码（居中）+ 版权信息（右对齐）

#### 4.2 更新reportService

```typescript
async generatePDF(reportId: string): Promise<Buffer> {
  const doc = new jsPDF()
  
  // 添加封面
  this.addCoverPage(doc, report)
  
  // 添加目录
  this.addTableOfContents(doc, report)
  
  // 添加内容页
  report.insights.forEach((insight, index) => {
    doc.addPage()
    this.addPageHeader(doc, report, index + 1)  // 新增：页眉
    this.addInsightSection(doc, insight)
    this.addPageFooter(doc, index + 1)  // 新增：页脚
  })
  
  return doc.output('arraybuffer')
}

private addCoverPage(doc: jsPDF, report: Report) {
  // Logo（居中）
  doc.addImage('/path/to/logo.png', 'PNG', 80, 40, 50, 15)
  
  // 标题（主色）
  doc.setFontSize(32)
  doc.setTextColor('#5E6AD2')
  doc.text(report.title, 105, 100, { align: 'center' })
  
  // 副标题
  doc.setFontSize(16)
  doc.setTextColor('#3A3A3A')
  doc.text(`生成日期：${format(new Date(), 'yyyy-MM-dd')}`, 105, 120, { align: 'center' })
}

private addPageHeader(doc: jsPDF, report: Report, pageNumber: number) {
  // Logo（左上角）
  doc.addImage('/path/to/logo.png', 'PNG', 10, 5, 30, 9)
  
  // 项目名称（中间）
  doc.setFontSize(10)
  doc.setTextColor('#3A3A3A')
  doc.text(report.project_name, 105, 10, { align: 'center' })
  
  // 生成日期（右上角）
  doc.text(format(new Date(), 'yyyy-MM-dd'), 190, 10, { align: 'right' })
  
  // 分隔线
  doc.setDrawColor('#E5E7EB')
  doc.line(10, 15, 200, 15)
}

private addPageFooter(doc: jsPDF, pageNumber: number) {
  // 分隔线
  doc.setDrawColor('#E5E7EB')
  doc.line(10, 282, 200, 282)
  
  // 页码（居中）
  doc.setFontSize(10)
  doc.setTextColor('#9CA3AF')
  doc.text(`${pageNumber}`, 105, 290, { align: 'center' })
  
  // 版权信息（右对齐）
  doc.text('© 2026 超级洞察. All rights reserved.', 190, 290, { align: 'right' })
}
```

**开发时长**: 3-4小时

**用户价值**:
- 专业度提升
- 品牌识别度增强
- PDF可直接用于客户汇报

**技术风险**: 低

**建议**: 可选，若时间充足则开发

---

### 5. 前端UI完整测试（优先级：低）

**功能描述**: 执行v2.2.2 Phase 3遗留的前端UI完整测试

**当前状态**: 
- 设计系统改造已完成
- 缺少Lighthouse评分和浏览器兼容性测试

**技术实现**:

#### 5.1 Lighthouse性能测试

**测试页面**:
- 首页（登录页）
- Workbench（数据上传）
- Insights（洞察生成）
- Topics（选题策划）
- Scripts（脚本创作）
- Report（报告导出）

**测试指标**:
- Performance: 目标≥90分
- Accessibility: 目标≥95分
- Best Practices: 目标≥90分
- SEO: 目标≥80分

**优化方向**:
- 图片懒加载
- 代码分割
- CSS压缩
- 字体优化

#### 5.2 浏览器兼容性测试

**测试浏览器**:
- Chrome 120+（主要）
- Safari 17+（macOS/iOS）
- Firefox 120+
- Edge 120+

**测试内容**:
- 样式渲染
- 交互功能
- 动画效果
- 深色模式

**开发时长**: 4小时

**用户价值**:
- 性能优化
- 兼容性保证

**技术风险**: 低

**建议**: 延后至v2.28.0（优先级低）

---

## 🎯 v2.27.0 最终功能清单

基于以上分析，v2.27.0最终功能清单如下：

### Phase 1: FTS5全文搜索升级（优先级：高）⭐

**目标**: 提升搜索性能50-70%，支持复杂查询

**开发内容**:
1. 创建FTS5虚拟表（Migration 16）
2. 创建Trigger同步机制（INSERT/UPDATE/DELETE）
3. 更新commentRepo.search()使用FTS5 MATCH
4. 迁移现有数据到comments_fts
5. 性能对比测试（LIKE vs FTS5）
6. 前端保持不变（透明升级）

**预计时长**: 3-4小时

**验证标准**:
- ✅ 搜索响应时间减少50-70%
- ✅ 支持短语搜索（"产品 卖点"）
- ✅ 支持布尔运算（产品 OR 卖点）
- ✅ 按相关性排序（bm25算法）
- ✅ Trigger同步验证（新增/更新/删除评论自动同步）

---

### Phase 2: 单元测试补充（优先级：高）⭐

**目标**: 测试覆盖率从40%提升至70%

**开发内容**:
1. **Repository层单元测试**（6-8小时）
   - commentRepo: 10个用例
   - notificationSettingsRepo: 6个用例
   - userRepo: 8个用例
   - projectRepo: 8个用例
   - insightRepo/topicRepo/scriptRepo: 各8个用例

2. **API集成测试**（可选，时间允许则开发）
   - comments.route: 15个用例
   - users.route: 10个用例
   - 其他routes: 各10个用例

**预计时长**: 6-10小时

**验证标准**:
- ✅ 测试覆盖率达到70%+
- ✅ 所有核心Repository有单元测试
- ✅ 所有测试用例通过
- ✅ CI/CD集成（GitHub Actions）

**阶段性交付**:
- Phase 2.1: Repository层测试（必做）- 6-8小时
- Phase 2.2: API集成测试（可选）- 4小时

---

### Phase 3: PDF导出品牌化（优先级：中，可选）

**目标**: 提升PDF专业度，完成v2.21.0遗留功能

**开发内容**:
1. 设计品牌元素（Logo、颜色主题）
2. 更新reportService（封面、页眉、页脚）
3. 测试PDF输出效果
4. 验证品牌一致性

**预计时长**: 3-4小时

**验证标准**:
- ✅ 封面包含Logo和主色
- ✅ 页眉包含项目名称和日期
- ✅ 页脚包含页码和版权信息
- ✅ 品牌颜色一致（#5E6AD2主色）

**执行条件**: Phase 1和Phase 2.1完成后，若时间充足则开发

---

### Phase 4: 测试与文档归档

**目标**: 确保功能质量，完成版本交付

**开发内容**:
1. 执行端到端测试（场景1：快消品完整流程）
2. 验证FTS5搜索性能
3. 验证单元测试覆盖率
4. 更新 package.json 版本号（2.26.0 → 2.27.0）
5. 创建 v2.27.0-RELEASE-NOTES.md
6. 创建 WORK-SUMMARY-v2.27.0.md

**预计时长**: 1小时

---

## 📊 开发资源评估

### 时间估算

| Phase | 功能 | 预计时长 | 优先级 |
|-------|------|---------|--------|
| Phase 1 | FTS5全文搜索升级 | 3-4小时 | 高⭐ |
| Phase 2.1 | Repository层单元测试 | 6-8小时 | 高⭐ |
| Phase 2.2 | API集成测试（可选） | 4小时 | 中 |
| Phase 3 | PDF导出品牌化（可选） | 3-4小时 | 中 |
| Phase 4 | 测试与文档归档 | 1小时 | 高 |
| **必做总计** | Phase 1 + 2.1 + 4 | **10-13小时** | - |
| **全部总计** | Phase 1-4 | **17-21小时** | - |

### 执行策略

**核心策略**: 聚焦高优先级功能，可选功能根据进度决定

**Phase执行顺序**:
1. **Phase 1**（必做）- FTS5搜索升级 → 用户价值高，技术风险中
2. **Phase 2.1**（必做）- Repository单元测试 → 技术债务，必须补充
3. **Phase 3**（可选）- PDF品牌化 → 若时间充足则开发
4. **Phase 2.2**（可选）- API集成测试 → 若时间充足则开发
5. **Phase 4**（必做）- 测试与文档归档 → 版本交付

**时间分配**:
- Day 1: Phase 1（3-4小时）
- Day 2-3: Phase 2.1（6-8小时）
- Day 4: Phase 3或Phase 2.2（根据进度）
- Day 5: Phase 4 + 可选功能补完

---

## 💡 技术决策

### 1. 为什么优先FTS5搜索升级？

**理由**:
- 用户价值高（搜索响应速度提升50-70%）
- 技术成熟（SQLite FTS5稳定）
- 透明升级（前端无需改动）
- 为未来扩展打基础（中文分词、语义搜索）

**风险**:
- 数据同步机制需要充分测试
- FTS5查询语法需要学习
- 现有数据迁移需要验证

**缓解措施**:
- 详细测试Trigger同步机制
- 编写详细的FTS5查询文档
- 迁移脚本添加数据验证

---

### 2. 为什么优先单元测试？

**理由**:
- 测试覆盖率仅40%（核心短板）
- 代码重构风险高（无测试保护）
- 技术债务积累（影响后续开发）
- 行业标准（70%+覆盖率）

**收益**:
- 代码质量提升
- 重构信心增强
- Bug减少30%
- 开发效率提升

---

### 3. 为什么延后搜索建议？

**理由**:
- 依赖搜索历史数据积累
- 用户价值相对较低
- 可在v2.28.0开发
- 当前版本聚焦性能和测试

---

### 4. 为什么PDF品牌化可选？

**理由**:
- v2.21.0已完成基础导出
- 品牌化非核心功能
- 优先级低于性能和测试
- 可在v2.28.0补充

---

## 📝 v2.28.0 预留功能

基于本次产品规划，以下功能延后至v2.28.0：

**优先级高**:
1. API集成测试补充（若v2.27.0未完成）
2. 中文分词支持（jieba集成）
3. 搜索建议（自动补全）

**优先级中**:
4. 标注增强（@提及/回复讨论/讨论面板）
5. 免打扰模式（工作时间外静音）
6. 前端UI完整测试（Lighthouse + 浏览器兼容性）

**优先级低**:
7. 用户反馈系统（v2.11.0遗留）
8. 国际化支持（i18n多语言）

---

## ✅ 产品规划结论

**v2.27.0 主题**: 搜索性能优化 + 测试覆盖率提升

**核心功能**:
1. ✅ FTS5全文搜索升级（Phase 1，优先级高）
2. ✅ Repository层单元测试（Phase 2.1，优先级高）
3. 🔄 PDF导出品牌化（Phase 3，可选）
4. 🔄 API集成测试（Phase 2.2，可选）
5. ✅ 测试与文档归档（Phase 4）

**必做功能时长**: 10-13小时（Phase 1 + 2.1 + 4）  
**全部功能时长**: 17-21小时

**用户价值**:
- 📈 搜索响应速度提升50-70%
- 📈 产品稳定性提升30%
- 📈 代码质量提升（测试覆盖率40% → 70%）
- 📈 专业度提升（PDF品牌化，可选）

**技术风险**: 中（FTS5数据同步机制需要充分测试）

**开发模式**: 自主开发（无需用户确认）

---

**规划完成日期**: 2026-04-12  
**下一步**: 进入Phase 1开发（FTS5全文搜索升级）
