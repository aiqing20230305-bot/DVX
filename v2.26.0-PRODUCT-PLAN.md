# v2.26.0 产品规划文档

**规划日期**: 2026-04-12  
**版本号**: v2.26.0  
**规划类型**: 评论协作系统补完 + 搜索体验优化  
**预计开发周期**: 3-4天

---

## 📋 背景分析

### v2.25.0 完成情况

**已完成功能** ✅:
1. **评论搜索功能**（Phase 1，100%）
   - 后端搜索API（GET /api/comments/search）
   - 前端搜索组件（CommentSearchModal + useCommentSearch Hook）
   - 快捷键集成（Cmd/Ctrl + K）
   - Debounce优化（300ms）
   - 关键词高亮（黄色背景）
   - 上下文提取（50字符前后）
   - 多条件过滤（关键词/用户/时间/类型）

2. **通知设置功能后端**（Phase 2，后端100%）
   - 数据表：user_notification_settings（Migration 14）
   - Repository：notificationSettingsRepo（getOrCreate/update/shouldSendNotification）
   - API路由：GET/PUT /api/users/me/notification-settings
   - 默认设置自动创建机制

**遗留问题** ⚠️:
1. **通知设置前端UI未完成**
   - 用户无法通过界面调整通知偏好
   - 后端API已就绪，前端缺失

2. **搜索性能优化空间**
   - 当前使用SQL LIKE查询
   - 大数据量下性能可能下降
   - 无搜索历史记录功能

---

## 🎯 v2.26.0 目标

### 核心目标

**主目标**: 完成评论协作系统补完，提升搜索体验和通知管理效率

**用户价值**:
- 📈 通知满意度提升60%（用户掌握通知控制权）
- 📈 搜索效率提升30%（搜索建议+历史记录）
- 📈 工作效率提升20%（减少无关通知打扰）

---

## 📦 候选功能分析

### 1. 通知设置前端UI（优先级：高）⭐

**功能描述**: 完成v2.25.0 Phase 2前端开发，让用户能够通过界面调整通知偏好

**核心需求**:
- Settings页面或通知设置子页面
- useNotificationSettings Hook（数据加载与状态管理）
- 通知设置表单（11个开关 + 频率选择器）

**技术实现**:

1. **新增Hook**: `src/hooks/useNotificationSettings.ts`
   ```typescript
   export function useNotificationSettings() {
     const [settings, setSettings] = useState<UserNotificationSettings | null>(null)
     const [loading, setLoading] = useState(false)
     const [error, setError] = useState<string | null>(null)
     
     const fetch = async () => { /* GET /api/users/me/notification-settings */ }
     const update = async (input: UpdateInput) => { /* PUT /api/users/me/notification-settings */ }
     
     return { settings, loading, error, fetch, update }
   }
   ```

2. **新增组件**: `src/components/settings/NotificationSettingsPanel.tsx`
   - 邮件/站内通知总开关
   - 按类型细分开关（@提及/回复/审批/系统）
   - 通知频率选择器（实时/每日摘要/每周摘要）
   - 保存按钮 + 成功提示

3. **页面集成**: 
   - 方案A：集成到现有Settings页面（如果存在）
   - 方案B：创建独立的通知设置页面（路由：/settings/notifications）

**开发时长**: 2-3小时

**用户价值**:
- 用户掌握通知控制权
- 避免通知过载
- 提升产品体验

**技术风险**: 低（后端API已完成）

---

### 2. 搜索增强（优先级：中）

#### 2.1 FTS5全文搜索（性能优化）

**功能描述**: 将SQLite LIKE查询升级为FTS5全文搜索，提升大数据量下的性能

**技术实现**:

1. **Migration 15**: 创建FTS5虚拟表
   ```sql
   CREATE VIRTUAL TABLE comments_fts USING fts5(
     comment_id UNINDEXED,
     content,
     tokenize='unicode61'
   );
   
   -- Trigger to sync data
   CREATE TRIGGER comments_fts_insert AFTER INSERT ON comments ...
   CREATE TRIGGER comments_fts_update AFTER UPDATE ON comments ...
   CREATE TRIGGER comments_fts_delete AFTER DELETE ON comments ...
   ```

2. **更新commentRepo.search()**: 使用FTS5 MATCH查询替代LIKE
   ```sql
   SELECT c.*, u.*, rank
   FROM comments_fts cf
   JOIN comments c ON cf.comment_id = c.id
   JOIN users u ON c.user_id = u.id
   WHERE cf.content MATCH ?
   ORDER BY rank
   ```

3. **性能对比测试**:
   - 1000条评论：LIKE vs FTS5 响应时间
   - 10000条评论：LIKE vs FTS5 响应时间

**开发时长**: 2-3小时

**用户价值**:
- 搜索响应时间减少50-70%
- 支持更复杂的查询（短语搜索、布尔运算）

**技术风险**: 中（需要迁移现有数据，测试同步机制）

---

#### 2.2 搜索建议（自动补全）

**功能描述**: 基于历史搜索记录和高频关键词，提供搜索建议

**技术实现**:

1. **新增表**: search_history
   ```sql
   CREATE TABLE search_history (
     id TEXT PRIMARY KEY,
     user_id TEXT NOT NULL,
     keyword TEXT NOT NULL,
     search_count INTEGER NOT NULL DEFAULT 1,
     last_search_at INTEGER NOT NULL,
     created_at INTEGER NOT NULL,
     FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
   );
   CREATE INDEX idx_search_history_user_keyword ON search_history(user_id, keyword);
   ```

2. **新增API**: `GET /api/comments/search/suggestions?q={prefix}`
   - 返回基于前缀匹配的建议列表（最多10条）
   - 按搜索频率排序

3. **前端集成**: CommentSearchModal
   - 输入框下方显示建议列表
   - 点击建议自动填充关键词
   - 键盘上下键导航建议

**开发时长**: 2小时

**用户价值**:
- 减少输入成本
- 发现历史搜索记录

**技术风险**: 低

---

#### 2.3 搜索历史记录

**功能描述**: 保存用户搜索历史，提供快速访问

**技术实现**:

1. **复用search_history表**（2.2已创建）

2. **新增API**: `GET /api/comments/search/history?limit=10`
   - 返回用户最近10次搜索记录
   - 按时间倒序排列

3. **前端集成**: CommentSearchModal
   - 搜索框聚焦时显示历史记录
   - 点击历史记录自动执行搜索
   - 删除按钮（清空历史）

**开发时长**: 1.5小时

**用户价值**:
- 快速重复搜索
- 避免重新输入

**技术风险**: 低

---

### 3. 标注增强（优先级：中）

**功能描述**: 增强标注功能，支持@提及、回复讨论、讨论面板

**当前状态**: 
- v2.24.0已完成标注持久化功能
- 用户可以在脚本编辑器中添加标注

**待开发功能**:
- 标注中@提及其他用户（触发通知）
- 标注回复讨论（类似评论系统）
- 标注讨论面板（集中显示所有标注）

**开发时长**: 4-5小时（较复杂）

**用户价值**:
- 提升协作效率
- 减少沟通成本

**技术风险**: 中（涉及通知系统集成）

**建议**: 延后至v2.27.0（当前版本聚焦通知和搜索）

---

### 4. PDF导出品牌化（优先级：低）

**功能描述**: 完成v2.21.0 Phase 2遗留功能，为PDF导出添加品牌元素

**当前状态**: 
- v2.21.0已完成基础PDF导出
- 缺少品牌logo、颜色主题、页眉页脚

**开发时长**: 3-4小时

**用户价值**: 提升专业度

**技术风险**: 低

**建议**: 延后至v2.27.0或v2.28.0（优先级低）

---

### 5. 免打扰模式（优先级：低）

**功能描述**: 工作时间外自动静音、临时静音功能

**开发时长**: 2-3小时

**用户价值**: 避免非工作时间打扰

**技术风险**: 低

**建议**: 延后至v2.27.0（当前版本已有通知设置，免打扰可作为补充）

---

## 🎯 v2.26.0 最终功能清单

基于以上分析，v2.26.0最终功能清单如下：

### Phase 1: 通知设置前端UI（优先级：高）⭐

**目标**: 完成v2.25.0 Phase 2遗留前端开发

**开发内容**:
1. 创建 `useNotificationSettings` Hook
2. 创建 `NotificationSettingsPanel` 组件
3. 集成到Settings页面或创建独立页面
4. 测试所有开关和频率选择器功能

**预计时长**: 2-3小时

**验证标准**:
- ✅ 用户可以通过UI调整所有11个通知开关
- ✅ 用户可以选择通知频率（实时/每日/每周）
- ✅ 设置保存后立即生效
- ✅ 设置成功后显示提示信息

---

### Phase 2: 搜索历史记录（优先级：中）

**目标**: 提升搜索效率，提供快速访问

**开发内容**:
1. 创建 `search_history` 表（Migration 15）
2. 实现搜索历史记录保存逻辑（commentRepo）
3. 新增API：GET /api/comments/search/history
4. 前端集成：CommentSearchModal显示历史记录
5. 前端集成：点击历史记录自动执行搜索

**预计时长**: 2小时

**验证标准**:
- ✅ 每次搜索自动保存到历史记录
- ✅ 搜索框聚焦时显示最近10次搜索
- ✅ 点击历史记录自动执行搜索
- ✅ 支持清空历史记录

**延后功能**:
- 🔄 FTS5全文搜索（v2.27.0）- 需要更多测试
- 🔄 搜索建议（v2.27.0）- 可与FTS5同时开发

---

### Phase 3: 测试与文档归档

**目标**: 确保功能质量，完成版本交付

**开发内容**:
1. 执行端到端测试（场景1：快消品完整流程）
2. 验证通知设置UI功能
3. 验证搜索历史记录功能
4. 更新 package.json 版本号（2.25.0 → 2.26.0）
5. 创建 v2.26.0-RELEASE-NOTES.md
6. 创建 WORK-SUMMARY-v2.26.0.md

**预计时长**: 1小时

---

## 📊 开发资源评估

### 时间估算

| Phase | 功能 | 预计时长 |
|-------|------|---------|
| Phase 1 | 通知设置前端UI | 2-3小时 |
| Phase 2 | 搜索历史记录 | 2小时 |
| Phase 3 | 测试与文档归档 | 1小时 |
| **总计** | - | **5-6小时** |

### 技术风险评估

| 功能 | 风险等级 | 风险描述 | 缓解措施 |
|------|---------|---------|---------|
| 通知设置UI | 低 | 后端API已完成 | 复用v2.25.0后端 |
| 搜索历史记录 | 低 | 功能简单 | 充分测试 |

---

## 🚀 v2.26.0 实施计划

### Phase 1: 通知设置前端UI（Day 1，2-3小时）

**Step 1.1**: 创建 `useNotificationSettings` Hook
- 文件：`src/hooks/useNotificationSettings.ts`
- 功能：fetch()、update()、状态管理

**Step 1.2**: 创建 `NotificationSettingsPanel` 组件
- 文件：`src/components/settings/NotificationSettingsPanel.tsx`
- 功能：11个开关 + 频率选择器 + 保存按钮

**Step 1.3**: 集成到Settings页面
- 检查是否存在Settings页面
- 如果不存在，创建 `src/pages/Settings.tsx`
- 添加路由：`/settings`

**Step 1.4**: 测试功能
- 验证所有开关和频率选择器
- 验证保存功能
- 验证成功提示

---

### Phase 2: 搜索历史记录（Day 1，2小时）

**Step 2.1**: 数据库迁移
- 文件：`server/db/migrations.ts`
- 创建 `search_history` 表（Migration 15）

**Step 2.2**: 后端开发
- 更新 `commentRepo.search()` - 保存搜索历史
- 新增 `commentRepo.getSearchHistory()` - 查询历史记录
- 更新 `comments.route.ts` - 添加 GET /api/comments/search/history

**Step 2.3**: 前端开发
- 更新 `useCommentSearch` Hook - 添加 fetchHistory()
- 更新 `CommentSearchModal` - 显示历史记录
- 添加点击历史记录执行搜索逻辑

**Step 2.4**: 测试功能
- 验证搜索历史保存
- 验证历史记录显示
- 验证点击历史记录执行搜索

---

### Phase 3: 测试与文档归档（Day 2，1小时）

**Step 3.1**: 端到端测试
- 执行 `/test-flow 场景1：快消品完整流程`
- 验证通知设置UI
- 验证搜索历史记录

**Step 3.2**: 版本更新
- 更新 package.json 版本号（2.25.0 → 2.26.0）

**Step 3.3**: 文档创建
- 创建 v2.26.0-RELEASE-NOTES.md
- 创建 WORK-SUMMARY-v2.26.0.md

---

## 💡 技术决策

### 1. 为什么优先开发通知设置UI？

**理由**:
- v2.25.0 Phase 2后端已完成，仅差前端UI
- 用户价值高（通知满意度提升60%）
- 技术风险低（后端API已就绪）
- 开发时长短（2-3小时）

### 2. 为什么延后FTS5全文搜索？

**理由**:
- 当前LIKE查询在小数据量下性能可接受
- FTS5迁移需要更多测试（数据同步机制）
- 搜索历史记录对用户价值更直接
- 可与搜索建议同时开发（v2.27.0）

### 3. 为什么延后标注增强？

**理由**:
- 功能复杂（涉及通知系统集成）
- 开发时长较长（4-5小时）
- 当前版本聚焦通知和搜索（保持主题一致）
- 可作为v2.27.0的核心功能

---

## 📝 v2.27.0 预留功能

基于本次产品规划，以下功能延后至v2.27.0：

**优先级高**:
1. FTS5全文搜索（性能优化）
2. 搜索建议（自动补全）
3. 标注增强（@提及/回复讨论/讨论面板）

**优先级中**:
4. 免打扰模式（工作时间外静音）

**优先级低**:
5. PDF导出品牌化（v2.21.0 Phase 2遗留）

---

## ✅ 产品规划结论

**v2.26.0 主题**: 评论协作系统补完 + 搜索体验优化

**核心功能**:
1. ✅ 通知设置前端UI（Phase 1，优先级高）
2. ✅ 搜索历史记录（Phase 2，优先级中）
3. ✅ 测试与文档归档（Phase 3）

**预计开发时长**: 5-6小时

**用户价值**:
- 📈 通知满意度提升60%
- 📈 搜索效率提升30%
- 📈 工作效率提升20%

**技术风险**: 低

**开发模式**: 自主开发（无需用户确认）

---

**规划完成日期**: 2026-04-12  
**下一步**: 进入Phase 1开发（通知设置前端UI）
