# v2.30.0 Phase 1: 测试隔离问题修复 - 进度报告

## 📋 任务信息

- **开始时间**: 2026-04-12
- **当前状态**: 进行中 (70-80%完成)
- **预计完成**: 2026-04-12

---

## 🎯 目标

解决多文件测试同时运行时的间歇性失败问题，实现100%稳定的测试通过率。

**问题现象**:
- 单独运行 `auth.route.test.ts` 或 `comments.route.test.ts` → 100% 通过
- 同时运行两个文件 → 30% 失败率（初始状态）

---

## 🔍 根本原因分析

### 已识别的问题

#### 1. ✅ 配置问题 - 测试并行执行
**问题**: vitest默认并行运行测试文件，导致资源竞争
**解决方案**: 在 `vitest.config.ts` 添加：
```typescript
test: {
  fileParallelism: false,  // 禁用文件级并行
  isolate: true,            // 隔离每个测试文件
}
```
**效果**: 初步改善，但未完全解决

---

#### 2. ✅ 数据库隔离问题
**问题**: API集成测试没有设置 `__TEST_DB__`，所有测试共享同一个数据库实例
**解决方案**: 创建 `test-db-setup.ts`，为每个测试文件创建独立的内存数据库

**实现**:
```typescript
// server/routes/__tests__/test-db-setup.ts
export function setupTestDatabase() {
  beforeAll(async () => {
    // 创建独立的内存数据库
    testDb = new Database(':memory:')
    testDb.pragma('journal_mode = WAL')
    testDb.pragma('foreign_keys = ON')
    
    // 加载schema
    const schema = readFileSync(schemaPath, 'utf-8')
    testDb.exec(schema)
    
    // 设置全局测试数据库
    ;(global as any).__TEST_DB__ = testDb
    
    // 运行迁移以创建FTS5表等
    await runMigrations()
  })
}
```

**集成到测试文件**:
```typescript
// auth.route.test.ts & comments.route.test.ts
import { setupTestDatabase } from './test-db-setup.js'

describe('API测试', () => {
  setupTestDatabase()  // 每个测试文件独立数据库
  // ...
})
```

**效果**: 解决了FTS5表缺失问题，但测试隔离仍未100%

---

#### 3. ✅ 数据清理不完整
**问题**: `cleanupTestData()` 只清理了5个表，遗漏了 `search_history` 等关键表
**症状**: 搜索历史测试失败，报错 `expected '设计' to be '技术'`

**解决方案**: 扩展 `cleanupTestData()` 覆盖所有用户相关表

**修复前**（仅5个表）:
```typescript
export function cleanupTestData() {
  db.prepare('DELETE FROM comments_fts').run()
  db.prepare('DELETE FROM comments').run()
  db.prepare('DELETE FROM project_members').run()
  db.prepare('DELETE FROM projects').run()
  db.prepare('DELETE FROM users').run()
}
```

**修复后**（18个表）:
```typescript
export function cleanupTestData() {
  // 1. 评论相关
  db.prepare('DELETE FROM comments_fts').run()
  db.prepare('DELETE FROM comments').run()

  // 2. 用户相关数据
  db.prepare('DELETE FROM search_history').run()
  db.prepare('DELETE FROM user_notification_settings').run()
  db.prepare('DELETE FROM notifications').run()
  db.prepare('DELETE FROM user_actions').run()
  db.prepare('DELETE FROM sessions').run()

  // 3. 项目相关数据
  db.prepare('DELETE FROM project_members').run()
  db.prepare('DELETE FROM uploads').run()
  db.prepare('DELETE FROM insights').run()
  db.prepare('DELETE FROM topics').run()

  // 4. 脚本相关数据
  db.prepare('DELETE FROM script_annotations').run()
  db.prepare('DELETE FROM script_history').run()
  db.prepare('DELETE FROM scripts').run()

  // 5. 审批相关数据
  db.prepare('DELETE FROM approval_reviews').run()
  db.prepare('DELETE FROM approval_requests').run()
  db.prepare('DELETE FROM approval_workflows').run()

  // 6. 其他数据
  db.prepare('DELETE FROM feedback').run()
  db.prepare('DELETE FROM reports').run()
  db.prepare('DELETE FROM logs').run()

  // 7. 主表
  db.prepare('DELETE FROM projects').run()
  db.prepare('DELETE FROM users').run()
}
```

**效果**: 正在验证中...

---

## 📊 改进效果对比

| 优化阶段 | 配置 | 通过率 | 说明 |
|---------|------|--------|------|
| **初始状态** | 默认并行 | ~70% | 基线测试 |
| **Stage 1** | fileParallelism: false | 70% | 顺序执行，无明显改善 |
| **Stage 2** | + 独立数据库 | 70% | FTS5表问题解决 |
| **Stage 3** | + 完整cleanup | **测试中** | 预期 90-100% |

---

## 🛠️ 已完成的修改

### 1. 配置文件
**文件**: `vitest.config.ts`
```typescript
export default defineConfig({
  test: {
    fileParallelism: false,  // ✅ 新增
    isolate: true,            // ✅ 新增
  },
})
```

---

### 2. 测试基础设施
**文件**: `server/routes/__tests__/test-db-setup.ts` (新建)
- 为每个测试文件创建独立内存数据库
- 自动运行迁移创建FTS5等表
- 确保测试文件之间完全隔离

---

### 3. 测试辅助函数
**文件**: `server/routes/__tests__/helpers.ts`
- 扩展 `cleanupTestData()` 从5个表增加到18个表
- 确保所有用户相关数据完全清理
- 按照外键依赖顺序删除，避免约束错误

---

### 4. 集成测试文件
**文件**: 
- `server/routes/__tests__/auth.route.test.ts`
- `server/routes/__tests__/comments.route.test.ts`

**修改**:
```typescript
import { setupTestDatabase } from './test-db-setup.js'

describe('API测试', () => {
  setupTestDatabase()  // ✅ 新增
  // ...
})
```

---

## 🔬 验证方法

### 当前验证任务
```bash
# 正在后台运行 30 次迭代测试
npm run test:run -- server/routes/__tests__/auth.route.test.ts server/routes/__tests__/comments.route.test.ts
```

### 验证标准
- ✅ **目标**: 30/30 通过 (100%)
- ⚠️ **可接受**: 28/30 通过 (93%+)
- ❌ **需进一步优化**: <27/30 (<90%)

---

## 🚧 遗留问题

### 潜在问题点

#### 1. Express app 状态共享
**症状**: 即使数据库隔离，仍有30%失败率
**怀疑**: Express app实例可能有共享状态（中间件、路由）
**缓解**: 使用 `fileParallelism: false` 强制顺序执行

#### 2. 异步timing问题
**症状**: 间歇性失败，无固定模式
**怀疑**: 某些异步操作（登录、数据库写入）可能未完全完成
**建议**: 添加显式等待或事务同步

#### 3. Node.js全局状态
**症状**: 不同测试文件间仍有干扰
**怀疑**: 可能有全局变量或单例模式未重置
**建议**: 审查所有单例模式和全局变量

---

## 📋 后续计划

### 如果当前修复达到 90%+
1. ✅ 接受当前方案，标记任务完成
2. 📝 更新 `TESTING-GUIDE.md` 添加测试隔离最佳实践
3. 📝 更新 `CHANGELOG.md` 记录 v2.30.0 Phase 1 完成
4. 🚀 进入 Phase 2: 测试覆盖补充

### 如果当前修复仍 <90%
**Option A**: 更激进的隔离方案
- 每个测试文件使用不同的临时数据库文件
- 使用进程级隔离（spawn子进程运行测试）

**Option B**: 接受当前状态
- 70% 成功率在CI环境可能更好（独立容器）
- 添加重试机制（vitest retry: 1）
- 标记为"已知限制"

**Option C**: 调整测试策略
- 拆分大测试文件为更小单元
- 减少测试间的共享状态
- 使用 mock 替代真实 API 调用

---

## 🎓 经验总结

### 测试隔离的关键要素

1. **数据库隔离** ✅
   - 每个测试文件独立数据库实例
   - 使用内存数据库加速测试
   - 运行完整迁移确保schema一致

2. **数据清理** ✅
   - 清理所有用户相关表（不仅主表）
   - 按照外键依赖顺序删除
   - 每个测试用例独立清理

3. **测试配置** ✅
   - `fileParallelism: false` 避免并行冲突
   - `isolate: true` 增强测试隔离
   - 明确的 beforeEach/afterEach 生命周期

4. **待改进** ⚠️
   - Express app 状态管理
   - 异步操作同步机制
   - 全局状态重置

---

**报告生成时间**: 2026-04-12  
**状态**: 进行中 - 等待30次迭代验证结果  
**预期完成**: 今日内
