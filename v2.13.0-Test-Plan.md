# v2.13.0 脚本模板系统 - 测试计划

**版本**: v2.13.0  
**功能**: Script Template System  
**创建日期**: 2026-04-12  
**状态**: 待执行

---

## 测试目标

验证脚本模板系统的以下核心功能：
1. **模板CRUD操作** - 创建、读取、更新、删除模板
2. **变量替换引擎** - 正确替换{变量名}为实际值
3. **A/B脚本生成** - 从模板生成A/B variant脚本
4. **全局模板支持** - project_id=NULL的全局模板
5. **模板统计** - 使用次数、热门模板、分类统计
6. **脚本保存为模板** - 将现有脚本转换为可复用模板
7. **API权限控制** - 认证中间件、权限验证

---

## 测试环境

- **数据库**: SQLite (`data.db`)
- **服务器**: Node.js + Express (http://localhost:3001)
- **前端**: React + Vite (http://localhost:5173)
- **测试工具**: curl, Postman (可选)
- **预置数据**: 3个官方模板（template_emotion_001, template_rational_001, template_harvest_001）

---

## 测试用例分类

### A. 模板CRUD操作测试

#### A1. 获取模板列表
**测试用例**: GET /api/templates  
**目的**: 验证模板列表查询和筛选功能

**测试步骤**:
```bash
# 1. 获取所有模板
curl http://localhost:3001/api/templates

# 2. 筛选全局模板
curl 'http://localhost:3001/api/templates?project_id=global'

# 3. 按分类筛选（情感型）
curl 'http://localhost:3001/api/templates?category=emotion'

# 4. 按平台筛选（抖音）
curl 'http://localhost:3001/api/templates?platform=douyin'

# 5. 搜索模板（关键词：室友）
curl 'http://localhost:3001/api/templates?search=室友'

# 6. 分页查询
curl 'http://localhost:3001/api/templates?limit=2&offset=0'
```

**预期结果**:
- 返回JSON数组包含templates和pagination
- 筛选条件正确应用
- 全局模板（project_id=null）正确返回
- 搜索匹配name/description/tags字段
- 分页参数正确限制结果数量

---

#### A2. 获取模板详情
**测试用例**: GET /api/templates/:id  
**目的**: 验证单个模板查询和变量提取

**测试步骤**:
```bash
# 1. 获取情感型模板详情
curl http://localhost:3001/api/templates/template_emotion_001

# 2. 获取不存在的模板
curl http://localhost:3001/api/templates/nonexistent_id
```

**预期结果**:
- 返回完整模板数据（包含segments JSON）
- 返回variables数组（从segments提取的{变量名}列表）
- 情感型模板应返回变量：["价格_高", "价格_低", "产品类别", "产品名", "核心卖点", ...]
- 不存在的模板返回404错误

---

#### A3. 创建新模板
**测试用例**: POST /api/templates  
**目的**: 验证模板创建功能

**测试步骤**:
```bash
# 1. 创建自定义模板
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试模板-洗发水推荐",
    "description": "适合洗发水产品的推荐模板",
    "category": "custom",
    "platform": "douyin",
    "segments": [
      {
        "type": "hook",
        "timing": "0-3s",
        "content": "{品牌名}的{产品名}真的绝了！",
        "direction": "产品特写镜头"
      },
      {
        "type": "cta",
        "timing": "15-20s",
        "content": "现在只要{价格}，快冲！",
        "direction": "购物车引导"
      }
    ],
    "tags": ["测试", "洗发水", "短视频"]
  }'

# 2. 缺少必填字段（name）
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{"segments": []}'

# 3. 缺少segments字段
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{"name": "测试"}'
```

**预期结果**:
- 成功创建返回201和完整模板数据
- 自动生成UUID作为id
- 自动设置created_at和updated_at
- usage_count初始化为0
- 缺少必填字段返回400错误

---

#### A4. 更新模板
**测试用例**: PUT /api/templates/:id  
**目的**: 验证模板更新功能

**测试步骤**:
```bash
# 1. 更新模板名称
curl -X PUT http://localhost:3001/api/templates/template_emotion_001 \
  -H "Content-Type: application/json" \
  -d '{"name": "情感共鸣型-室友对比（已更新）"}'

# 2. 更新segments
curl -X PUT http://localhost:3001/api/templates/template_emotion_001 \
  -H "Content-Type: application/json" \
  -d '{
    "segments": [
      {
        "type": "hook",
        "timing": "0-3s",
        "content": "更新后的文案",
        "direction": "更新后的镜头"
      }
    ]
  }'

# 3. 更新不存在的模板
curl -X PUT http://localhost:3001/api/templates/nonexistent_id \
  -H "Content-Type: application/json" \
  -d '{"name": "测试"}'
```

**预期结果**:
- 成功更新返回200和更新后的模板
- updated_at字段更新为当前时间
- 部分字段更新（只更新提供的字段）
- 不存在的模板返回404错误

---

#### A5. 删除模板
**测试用例**: DELETE /api/templates/:id  
**目的**: 验证模板删除功能

**测试步骤**:
```bash
# 1. 删除自定义模板（先创建一个）
# 创建
TEMPLATE_ID=$(curl -s -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{"name":"待删除模板","segments":[{"type":"hook","timing":"0-3s","content":"test","direction":"test"}]}' \
  | jq -r '.template.id')

# 删除
curl -X DELETE http://localhost:3001/api/templates/$TEMPLATE_ID

# 验证已删除
curl http://localhost:3001/api/templates/$TEMPLATE_ID

# 2. 删除不存在的模板
curl -X DELETE http://localhost:3001/api/templates/nonexistent_id
```

**预期结果**:
- 成功删除返回200和{success: true}
- 删除后查询返回404
- 不存在的模板返回404错误

---

### B. 变量替换引擎测试

#### B1. 基础变量替换
**测试用例**: POST /api/templates/:id/apply  
**目的**: 验证{变量名}正确替换为实际值

**测试步骤**:
```bash
# 1. 使用情感型模板生成脚本（完整变量）
curl -X POST http://localhost:3001/api/templates/template_emotion_001/apply \
  -H "Content-Type: application/json" \
  -d '{
    "topicId": "topic_test_001",
    "projectId": "project_test_001",
    "variables": {
      "价格_高": "199元",
      "价格_低": "49元",
      "产品类别": "洗发水",
      "产品名": "多芬无硅油洗发水",
      "核心卖点": "无硅油配方，温和不伤发",
      "竞品描述": "大牌洗发水",
      "核心卖点对比": "她的含硅油，我的无硅油更健康",
      "规格说明": "500ml大容量",
      "使用场景": "每天洗头",
      "性价比计算": "每次只要1块钱",
      "使用时长": "一个月",
      "效果验证": "头发顺滑不打结",
      "品牌背书": "多芬品牌60年历史",
      "误区": "贵就是好"
    },
    "saveToDatabase": false
  }'
```

**预期结果**:
- 返回scriptA和scriptB两个完整脚本对象
- 所有{变量名}被正确替换为实际值
- segments中的content和direction字段都完成替换
- full_text字段为所有segment content的拼接
- word_count字段为full_text的字符数
- 不保存到数据库（saveToDatabase: false）

**验证点**:
检查返回的scriptA.segments[0].voiceover是否为：
```
"室友花199元买的洗发水，我49元的效果比她还好"
```

---

#### B2. 部分变量替换
**测试用例**: 部分变量提供，其余保留原样  
**目的**: 验证允许部分变量不填

**测试步骤**:
```bash
# 仅提供部分变量
curl -X POST http://localhost:3001/api/templates/template_emotion_001/apply \
  -H "Content-Type: application/json" \
  -d '{
    "topicId": "topic_test_002",
    "projectId": "project_test_001",
    "variables": {
      "价格_高": "299元",
      "价格_低": "79元",
      "产品类别": "面霜"
    },
    "saveToDatabase": false
  }'
```

**预期结果**:
- 提供的变量正确替换
- 未提供的变量保留{变量名}格式
- 不报错，允许部分替换

**验证点**:
返回的content应包含：
- "299元" 和 "79元"（已替换）
- "{产品名}" 和 "{核心卖点}"（未替换）

---

#### B3. 空变量测试
**测试用例**: 不提供variables参数  
**目的**: 验证默认行为

**测试步骤**:
```bash
# 不提供variables
curl -X POST http://localhost:3001/api/templates/template_harvest_001/apply \
  -H "Content-Type: application/json" \
  -d '{
    "topicId": "topic_test_003",
    "projectId": "project_test_001",
    "saveToDatabase": false
  }'
```

**预期结果**:
- 所有{变量名}保持不变
- 生成的脚本可用于后续手动编辑

---

#### B4. 变量提取测试
**测试用例**: GET /api/templates/:id（提取variables字段）  
**目的**: 验证从模板中正确提取所有变量

**测试步骤**:
```bash
# 获取情感型模板的变量列表
curl http://localhost:3001/api/templates/template_emotion_001 | jq '.variables'
```

**预期结果**:
返回数组包含所有变量（去重）：
```json
[
  "价格_高",
  "价格_低",
  "产品类别",
  "产品名",
  "核心卖点",
  "竞品描述",
  "核心卖点对比",
  "规格说明",
  "使用场景",
  "性价比计算",
  "使用时长",
  "效果验证",
  "品牌背书",
  "价值主张",
  "误区"
]
```

---

### C. 脚本生成与保存测试

#### C1. 生成并保存脚本
**测试用例**: POST /api/templates/:id/apply（saveToDatabase: true）  
**目的**: 验证从模板生成脚本并保存到数据库

**前置条件**:
```bash
# 创建测试项目
PROJECT_ID=$(curl -s -X POST http://localhost:3001/api/project \
  -H "Content-Type: application/json" \
  -d '{"name":"模板测试项目"}' \
  | jq -r '.project.id')

# 创建测试选题
TOPIC_ID=$(curl -s -X POST http://localhost:3001/api/topic \
  -H "Content-Type: application/json" \
  -d "{\"project_id\":\"$PROJECT_ID\",\"title\":\"测试选题\",\"platform\":\"douyin\"}" \
  | jq -r '.topic.id')
```

**测试步骤**:
```bash
# 使用模板生成并保存脚本
curl -X POST http://localhost:3001/api/templates/template_harvest_001/apply \
  -H "Content-Type: application/json" \
  -d "{
    \"topicId\": \"$TOPIC_ID\",
    \"projectId\": \"$PROJECT_ID\",
    \"variables\": {
      \"产品名\": \"多芬洗发水\",
      \"核心卖点\": \"无硅油配方\",
      \"3个核心卖点\": \"无硅油、温和、大容量\",
      \"价格\": \"49元\",
      \"规格\": \"500ml\",
      \"使用场景\": \"每天洗头\",
      \"效果展示\": \"头发柔顺有光泽\",
      \"对比前后\": \"对比一周前的毛躁\",
      \"限时优惠\": \"今天下单立减10元\",
      \"购买理由\": \"性价比超高\"
    },
    \"saveToDatabase\": true
  }"

# 验证脚本已保存
curl "http://localhost:3001/api/script?project_id=$PROJECT_ID"
```

**预期结果**:
- 返回2个已保存的脚本对象（scriptA和scriptB）
- 脚本关联到正确的project_id和topic_id
- variant字段分别为'A'和'B'
- 数据库中可查询到这2个脚本
- 模板的usage_count增加1

---

#### C2. 脚本保存为模板
**测试用例**: POST /api/scripts/:id/save-as-template  
**目的**: 验证将现有脚本转换为可复用模板

**前置条件**:
使用C1创建的脚本，或创建新脚本

**测试步骤**:
```bash
# 1. 获取现有脚本ID
SCRIPT_ID=$(curl -s "http://localhost:3001/api/script?project_id=$PROJECT_ID" \
  | jq -r '.scripts[0].id')

# 2. 将脚本保存为模板
curl -X POST http://localhost:3001/api/scripts/$SCRIPT_ID/save-as-template \
  -H "Content-Type: application/json" \
  -d '{
    "name": "从脚本生成的模板",
    "description": "基于实际脚本创建的可复用模板",
    "category": "custom",
    "platform": "douyin",
    "tags": ["测试", "从脚本生成"]
  }'

# 3. 验证模板已创建
curl http://localhost:3001/api/templates | jq '.templates[] | select(.name == "从脚本生成的模板")'

# 4. 使用错误的script_id
curl -X POST http://localhost:3001/api/scripts/nonexistent_id/save-as-template \
  -H "Content-Type: application/json" \
  -d '{"name":"测试"}'
```

**预期结果**:
- 成功创建返回201和新模板
- 模板的segments字段从脚本的segments解析而来
- source_script_id字段记录原脚本ID
- project_id继承自原脚本
- 不存在的脚本返回404错误

---

### D. 模板统计测试

#### D1. 模板统计信息
**测试用例**: GET /api/templates/stats  
**目的**: 验证模板统计功能

**测试步骤**:
```bash
# 1. 获取全局统计
curl http://localhost:3001/api/templates/stats

# 2. 获取特定项目统计
curl "http://localhost:3001/api/templates/stats?project_id=$PROJECT_ID"
```

**预期结果**:
返回JSON包含：
- total: 模板总数
- categoryStats: 各分类模板数量数组
  ```json
  [
    {"category": "emotion", "count": 1},
    {"category": "rational", "count": 1},
    {"category": "harvest", "count": 1},
    {"category": "custom", "count": X}
  ]
  ```
- popular: 前5个热门模板（按usage_count降序）

---

#### D2. 热门模板查询
**测试用例**: 验证usage_count统计  
**目的**: 确认使用次数正确增加

**测试步骤**:
```bash
# 1. 查看初始usage_count
INITIAL_COUNT=$(curl -s http://localhost:3001/api/templates/template_harvest_001 \
  | jq '.template.usage_count')

# 2. 使用模板生成脚本
curl -X POST http://localhost:3001/api/templates/template_harvest_001/apply \
  -H "Content-Type: application/json" \
  -d "{
    \"topicId\": \"$TOPIC_ID\",
    \"projectId\": \"$PROJECT_ID\",
    \"variables\": {},
    \"saveToDatabase\": false
  }"

# 3. 查看更新后的usage_count
NEW_COUNT=$(curl -s http://localhost:3001/api/templates/template_harvest_001 \
  | jq '.template.usage_count')

# 4. 验证增加了1
echo "Initial: $INITIAL_COUNT, New: $NEW_COUNT"
```

**预期结果**:
- NEW_COUNT = INITIAL_COUNT + 1
- 无论saveToDatabase为true或false，usage_count都应增加

---

### E. 权限与错误处理测试

#### E1. 认证中间件测试
**测试用例**: 验证auth中间件  
**目的**: 确保需要认证的endpoints正确阻止未授权访问

**测试步骤**:
```bash
# 注意：当前routes/template.routes.ts所有endpoints都使用auth中间件
# 如果没有有效token，应该返回401

# 测试未认证访问（如果实现了token验证）
# 当前实现可能允许所有请求通过（简化实现）
# 生产环境应添加JWT token验证
```

**预期结果**:
- 生产环境应返回401 Unauthorized
- 开发环境可能允许通过（取决于auth中间件实现）

---

#### E2. 权限控制测试
**测试用例**: 验证创建人权限  
**目的**: 确保只有创建人可编辑/删除模板

**当前状态**:
routes/template.routes.ts中有权限检查代码但被注释掉（简化实现）：
```typescript
// 暂时允许所有人编辑（简化实现）
// return res.status(403).json({ error: 'Permission denied' })
```

**未来测试**（当权限启用后）:
- 用户A创建模板
- 用户B尝试编辑 → 应返回403
- 用户A编辑 → 应成功
- 用户B尝试删除 → 应返回403

---

#### E3. 错误输入处理
**测试用例**: 验证各种错误输入  
**目的**: 确保API健壮性

**测试步骤**:
```bash
# 1. 无效的JSON
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{invalid json}'

# 2. segments不是数组
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{"name":"test","segments":"not an array"}'

# 3. 空数组segments
curl -X POST http://localhost:3001/api/templates \
  -H "Content-Type: application/json" \
  -d '{"name":"test","segments":[]}'

# 4. apply时缺少必填参数
curl -X POST http://localhost:3001/api/templates/template_emotion_001/apply \
  -H "Content-Type: application/json" \
  -d '{"variables":{}}'

# 5. 无效的模板ID格式
curl http://localhost:3001/api/templates/'; DROP TABLE script_templates;--'
```

**预期结果**:
- 无效JSON返回400错误
- 空segments数组返回400错误
- 缺少topicId/projectId返回400错误
- 所有错误有明确的error消息
- SQL注入攻击无效（参数化查询防护）

---

### F. 集成测试

#### F1. 完整工作流测试
**测试场景**: 模拟用户完整使用流程  
**目的**: 验证端到端功能

**测试步骤**:
```bash
# 1. 创建项目
PROJECT_ID=$(curl -s -X POST http://localhost:3001/api/project \
  -H "Content-Type: application/json" \
  -d '{"name":"多芬洗发水推广项目"}' \
  | jq -r '.project.id')

# 2. 浏览模板
curl -s http://localhost:3001/api/templates | jq '.templates[] | {id, name, category}'

# 3. 查看情感型模板详情
curl -s http://localhost:3001/api/templates/template_emotion_001 | jq '{name, variables}'

# 4. 创建选题
TOPIC_ID=$(curl -s -X POST http://localhost:3001/api/topic \
  -H "Content-Type: application/json" \
  -d "{
    \"project_id\": \"$PROJECT_ID\",
    \"title\": \"多芬无硅油洗发水室友对比\",
    \"platform\": \"douyin\"
  }" | jq -r '.topic.id')

# 5. 使用模板生成脚本
SCRIPTS=$(curl -s -X POST http://localhost:3001/api/templates/template_emotion_001/apply \
  -H "Content-Type: application/json" \
  -d "{
    \"topicId\": \"$TOPIC_ID\",
    \"projectId\": \"$PROJECT_ID\",
    \"variables\": {
      \"价格_高\": \"199元\",
      \"价格_低\": \"49元\",
      \"产品类别\": \"洗发水\",
      \"产品名\": \"多芬无硅油洗发水\",
      \"核心卖点\": \"无硅油配方，温和不刺激\",
      \"竞品描述\": \"某大牌洗发水\",
      \"核心卖点对比\": \"她的含硅油会假滑，我的无硅油真健康\",
      \"规格说明\": \"500ml大容量\",
      \"使用场景\": \"每天早晚洗头\",
      \"性价比计算\": \"每次只要不到2块钱\",
      \"使用时长\": \"一个月\",
      \"效果验证\": \"头发从毛躁变顺滑，发质明显改善\",
      \"品牌背书\": \"多芬品牌60年历史，全球销量No.1\",
      \"价值主张\": \"好用的洗发水不一定贵\",
      \"误区\": \"贵的就是好的\"
    },
    \"saveToDatabase\": true
  }")

echo "$SCRIPTS" | jq '.scripts[] | {variant, full_text}'

# 6. 查看项目脚本列表
curl -s "http://localhost:3001/api/script?project_id=$PROJECT_ID" | jq '.scripts | length'

# 7. 将脚本A保存为新模板
SCRIPT_A_ID=$(echo "$SCRIPTS" | jq -r '.scripts[0].id')
NEW_TEMPLATE=$(curl -s -X POST http://localhost:3001/api/scripts/$SCRIPT_A_ID/save-as-template \
  -H "Content-Type: application/json" \
  -d '{
    "name": "多芬洗发水-室友对比模板",
    "description": "基于实际脚本优化的洗发水推广模板",
    "category": "custom",
    "platform": "douyin",
    "tags": ["多芬", "洗发水", "室友对比", "性价比"]
  }')

echo "$NEW_TEMPLATE" | jq '{id, name, usage_count}'

# 8. 查看项目模板统计
curl -s "http://localhost:3001/api/templates/stats?project_id=$PROJECT_ID" \
  | jq '{total, categoryStats}'
```

**预期结果**:
- 所有步骤顺利执行
- 生成2个脚本（A和B）
- 脚本A保存为新模板成功
- 统计数据正确更新

---

#### F2. 全局模板与项目模板混合查询
**测试用例**: 验证全局模板正确显示  
**目的**: 确保project_id筛选逻辑正确

**测试步骤**:
```bash
# 1. 查询全局模板（应返回3个官方模板）
curl -s 'http://localhost:3001/api/templates?project_id=global' | jq '.templates | length'

# 2. 查询特定项目模板（应包含项目模板+全局模板）
curl -s "http://localhost:3001/api/templates?project_id=$PROJECT_ID" \
  | jq '.templates[] | {name, project_id}'

# 3. 查询所有模板（不限项目）
curl -s 'http://localhost:3001/api/templates' | jq '.templates | length'
```

**预期结果**:
- project_id=global: 仅返回project_id为null的模板
- project_id=具体ID: 返回该项目的模板 + 全局模板
- 无project_id参数: 返回所有模板

---

### G. 性能测试

#### G1. 大量模板查询性能
**测试用例**: 查询响应时间  
**目的**: 验证索引优化效果

**测试步骤**:
```bash
# 测试查询响应时间
time curl -s 'http://localhost:3001/api/templates?limit=100' > /dev/null
```

**预期结果**:
- 查询时间 < 100ms（数据库有5个索引优化）

---

#### G2. 变量替换性能
**测试用例**: 大量变量替换  
**目的**: 验证replaceVariables函数性能

**测试步骤**:
创建包含100个变量的模板，测试替换时间

**预期结果**:
- 单次替换时间 < 50ms

---

## 测试执行计划

### 阶段1: 单元测试（15分钟）
- [ ] A1-A5: 模板CRUD操作
- [ ] B1-B4: 变量替换引擎
- [ ] D1-D2: 模板统计

### 阶段2: 集成测试（10分钟）
- [ ] C1-C2: 脚本生成与保存
- [ ] F1-F2: 完整工作流

### 阶段3: 错误处理测试（5分钟）
- [ ] E1-E3: 权限与错误处理

### 阶段4: 性能测试（5分钟）
- [ ] G1-G2: 性能验证

**总计**: 约35分钟

---

## 测试报告模板

### 测试结果
| 测试用例 | 状态 | 执行时间 | 备注 |
|---------|------|---------|------|
| A1 | ✅/❌ | Xms | |
| A2 | ✅/❌ | Xms | |
| ... | ... | ... | |

### 发现的问题
1. **问题描述**: ...
   - **严重程度**: P0/P1/P2
   - **复现步骤**: ...
   - **建议修复**: ...

### 性能指标
- 平均查询响应时间: Xms
- 变量替换时间: Xms
- 端到端工作流时间: Xs

### 测试覆盖率
- API endpoints: X/7 (100%)
- 核心功能: X/6 (100%)
- 错误场景: X/5 (100%)

---

## 测试通过标准

✅ **必须通过**:
- 所有CRUD操作正常
- 变量替换引擎正确工作
- A/B脚本生成成功
- 全局模板正确显示
- 统计数据准确

⚠️ **可接受问题**:
- 权限控制未完全实现（简化版本）
- 部分性能优化待后续版本

❌ **阻塞发布**:
- 数据库错误
- API 500错误
- 变量替换失败
- 生成脚本格式错误

---

**测试负责人**: Claude (Autonomous Agent)  
**预计执行时间**: 35分钟  
**状态**: 待执行
