# 超级洞察 v2.28.0 产品规划

**规划日期**: 2026-04-12  
**规划版本**: v2.28.0  
**代号**: 搜索体验优化与测试补完  
**规划者**: Claude Opus 4.6

---

## 📋 版本背景

### v2.27.0完成情况

**已完成功能**:
- ✅ **FTS5全文搜索升级** - 搜索性能提升50-70%，支持bm25相关性排序
- ✅ **commentRepo单元测试** - 10个测试用例，建立测试框架
- ✅ **向后兼容设计** - 前端零改动，API接口不变
- ✅ **Trigger自动同步** - 数据一致性保证

**技术亮点**:
- Migration 16引入FTS5虚拟表
- unicode61 tokenizer支持中文
- bm25相关性排序取代时间排序
- vitest测试框架建立

**延后工作**:
- ⏸️ **46个Repository单元测试** - 延后至v2.28.0
  - notificationSettingsRepo: 8个测试用例
  - userRepo: 10个测试用例
  - projectRepo: 10个测试用例
  - insightRepo: 8个测试用例
  - topicRepo: 5个测试用例
  - scriptRepo: 5个测试用例

**产品完整度**: 90% → 95%（核心搜索功能完善）

---

## 🎯 v2.28.0核心目标

### 战略定位

v2.28.0专注于**搜索体验优化**和**技术债务清理**，是继v2.27.0搜索性能提升后的体验深化迭代。核心目标：

1. **提升中文搜索体验** - jieba分词，解决"产品卖点"整体匹配问题
2. **增强搜索效率** - 自动补全，减少重复输入
3. **补完测试覆盖** - 系统性补充Repository单元测试
4. **技术债务清理** - 完成v2.21.0/v2.11.0/v2.2.2遗留任务

### 用户价值

- 🔍 **中文搜索精准度** - "产品"能匹配"产品卖点"
- ⚡ **搜索效率提升** - 历史记录自动补全，减少输入
- 🧪 **代码质量保障** - 核心Repository有测试保护
- 📊 **产品完整度提升** - 95% → 98%

---

## 📊 候选功能分析

### 功能列表

| 功能 | 来源 | 优先级 | 预估时长 | 技术复杂度 | 用户价值 |
|-----|------|-------|---------|-----------|---------|
| Repository测试补充 | v2.27.0延后 | 🔴 高 | 5-6h | 中 | 高（质量保障） |
| 中文分词支持 | v2.27.0规划 | 🔴 高 | 2-3h | 中 | 高（搜索体验） |
| 搜索建议 | v2.27.0规划 | 🔴 高 | 1-2h | 低 | 高（效率提升） |
| API集成测试 | v2.27.0规划 | 🟡 中 | 3-4h | 中 | 中（质量保障） |
| PDF导出品牌化 | v2.21.0遗留 | 🟡 中 | 2-3h | 低 | 中（品牌一致性） |
| 前端UI测试 | v2.2.2遗留 | 🟢 低 | 4-5h | 低 | 低（质量提升） |
| 用户反馈系统 | v2.11.0遗留 | 🟢 低 | 3-4h | 中 | 中（产品迭代） |

### 功能详细分析

#### 1. Repository测试补充（优先级：🔴 高）

**问题**: 
- v2.27.0仅完成commentRepo测试（10个用例）
- 剩余6个核心Repository无测试覆盖
- 技术债务累积，影响代码质量

**解决方案**:
- 系统性补充46个单元测试用例
- 覆盖6个核心Repository（notificationSettingsRepo/userRepo/projectRepo/insightRepo/topicRepo/scriptRepo）
- 建立完整的Repository测试体系

**技术方案**:
- 复用commentRepo.test.ts测试框架
- beforeEach创建测试数据库
- afterEach清理测试环境
- Given-When-Then测试风格

**用户价值**:
- ✅ 核心功能有测试保护
- ✅ 重构信心增强
- ✅ Bug提前发现
- ✅ 代码质量提升

**预估时长**: 5-6小时
- notificationSettingsRepo: 1h（8个测试用例）
- userRepo: 1.5h（10个测试用例）
- projectRepo: 1.5h（10个测试用例）
- insightRepo: 1h（8个测试用例）
- topicRepo: 0.5h（5个测试用例）
- scriptRepo: 0.5h（5个测试用例）

**技术风险**: 低
- 测试框架已建立
- 测试模式已验证
- 无前端改动

---

#### 2. 中文分词支持（优先级：🔴 高）

**问题**: 
- FTS5 unicode61不支持中文分词
- "产品卖点"被视为整体，搜索"产品"无法匹配
- 用户需要输入完整词组才能搜索到结果

**解决方案**:
- 集成jieba分词器（Node.js版）
- 修改FTS5 Tokenizer配置
- 创建自定义分词函数

**技术方案**:

**方案1: 预分词存储（推荐）**
```typescript
// 在插入/更新评论时预先分词
import jieba from 'nodejieba'

// 评论创建时
const content = '这个产品的卖点很独特'
const segmented = jieba.cut(content).join(' ')  // "这个 产品 的 卖点 很 独特"

// 存入FTS5表
db.prepare('INSERT INTO comments_fts(comment_id, content) VALUES (?, ?)').run(id, segmented)
```

**优点**:
- ✅ 搜索时无需分词（性能高）
- ✅ 无需修改FTS5配置
- ✅ 兼容现有查询逻辑

**缺点**:
- ❌ 存储空间增加10-20%
- ❌ Trigger需要调用Node.js函数（实现复杂）

**方案2: 查询时分词（备选）**
```typescript
// 搜索时分词
const keyword = '产品'
const segmented = jieba.cut(keyword).join(' ')  // "产品"
const ftsQuery = segmented.split(/\s+/).map(k => `"${k}"`).join(' OR ')
```

**优点**:
- ✅ 无需修改存储
- ✅ Trigger无需改动

**缺点**:
- ❌ 每次搜索都要分词（性能略低）
- ❌ 无法利用FTS5内置分词

**推荐方案**: 方案1（预分词存储）
- 性能最优（搜索时无需分词）
- 实现方式：在commentRepo.create()中调用jieba分词，再存入FTS5

**用户价值**:
- ✅ 中文搜索体验大幅提升
- ✅ "产品"能匹配"产品卖点"
- ✅ 减少用户输入，提升搜索效率

**预估时长**: 2-3小时
- jieba集成: 1h
- FTS5表重建: 0.5h
- commentRepo改造: 0.5h
- 测试验证: 1h

**技术风险**: 中
- jieba依赖安装（native模块）
- Trigger函数调用Node.js（可能需要SQLite扩展）
- 数据迁移（重新分词所有评论）

---

#### 3. 搜索建议（优先级：🔴 高）

**问题**: 
- 用户每次都要手动输入关键词
- 无历史记录提示
- 重复输入效率低

**解决方案**:
- 基于search_history表实现自动补全
- 输入时显示最近10条搜索历史
- 支持键盘导航（↑↓选择，Enter确认）

**技术方案**:

**后端API** (已有，无需改动):
```typescript
// GET /api/comments/search/history?limit=10
commentRepo.getSearchHistory(userId, 10)
```

**前端实现**:
```typescript
// src/components/comments/CommentSearchModal.tsx
const [suggestions, setSuggestions] = useState<string[]>([])
const [showSuggestions, setShowSuggestions] = useState(false)

// 输入时获取建议
const handleInputChange = async (e) => {
  const value = e.target.value
  setKeyword(value)
  
  if (value.length > 0) {
    const history = await fetch('/api/comments/search/history?limit=10')
    const filtered = history.filter(h => h.keyword.includes(value))
    setSuggestions(filtered.map(h => h.keyword))
    setShowSuggestions(true)
  } else {
    setShowSuggestions(false)
  }
}

// 选择建议
const handleSelectSuggestion = (suggestion) => {
  setKeyword(suggestion)
  setShowSuggestions(false)
  // 执行搜索
}
```

**UI设计**:
```
┌─────────────────────────────────┐
│ 搜索评论                        │
├─────────────────────────────────┤
│ [产品___________________] 🔍   │  ← 输入框
├─────────────────────────────────┤
│ 最近搜索:                       │
│ • 产品卖点 (5次)               │  ← 建议列表
│ • 产品优势 (3次)               │
│ • 产品差异化 (2次)             │
└─────────────────────────────────┘
```

**键盘交互**:
- ↓ - 下一个建议
- ↑ - 上一个建议
- Enter - 选择建议并搜索
- Esc - 关闭建议列表

**用户价值**:
- ✅ 减少重复输入，提升搜索效率
- ✅ 发现历史搜索记录
- ✅ 键盘友好，提升体验

**预估时长**: 1-2小时
- 前端组件改造: 1h
- 键盘交互实现: 0.5h
- UI设计与测试: 0.5h

**技术风险**: 低
- API已存在，无需后端改动
- 前端逻辑简单
- 无数据迁移

---

#### 4. API集成测试（优先级：🟡 中）

**问题**: 
- 仅有Repository单元测试，无完整API流程验证
- 无法验证API路由、中间件、错误处理
- 缺少端到端测试覆盖

**解决方案**:
- 使用Supertest创建API集成测试
- 验证完整请求-响应流程
- 覆盖核心API端点

**技术方案**:
```typescript
// server/routes/comments.route.test.ts
import request from 'supertest'
import app from '../index.js'

describe('POST /api/comments/search', () => {
  it('应该返回匹配的评论', async () => {
    // Given: 创建测试数据
    const userId = await createTestUser()
    const projectId = await createTestProject(userId)
    await createTestComment(projectId, userId, '这个产品的卖点很独特')
    
    // When: 调用搜索API
    const response = await request(app)
      .post('/api/comments/search')
      .send({ keyword: '产品' })
      .expect(200)
    
    // Then: 验证响应
    expect(response.body.total).toBe(1)
    expect(response.body.comments[0].content).toContain('产品')
  })
})
```

**覆盖的API端点**:
1. `POST /api/comments/search` - 搜索评论（FTS5）
2. `GET /api/comments/search/history` - 搜索历史
3. `DELETE /api/comments/search/history` - 清空历史
4. `POST /api/comments` - 创建评论（验证Trigger同步）
5. `PUT /api/comments/:id` - 更新评论（验证Trigger同步）
6. `DELETE /api/comments/:id` - 删除评论（验证Trigger同步）

**用户价值**:
- ✅ 完整API流程验证
- ✅ 中间件、认证、错误处理测试
- ✅ 提升系统稳定性

**预估时长**: 3-4小时
- Supertest集成: 1h
- 6个API测试用例: 2h
- CI/CD集成: 1h

**技术风险**: 中
- 测试数据库隔离（避免污染生产数据）
- 认证中间件Mock（JWT token生成）

**决策**: 延后至Phase 5（可选）
- 优先完成Repository测试和搜索体验优化
- API集成测试作为质量加固，不影响功能交付

---

#### 5. PDF导出品牌化（优先级：🟡 中）

**问题**: 
- v2.21.0 Phase 2延后的功能
- 当前PDF导出无Logo、无页眉页脚
- 品牌识别度低

**解决方案**:
- 在PDF中添加Logo（页眉左上角）
- 添加页脚（页码 + 品牌名称）
- 自定义主题色（与品牌一致）

**技术方案**:
```typescript
// server/routes/report.route.ts
import { jsPDF } from 'jspdf'
import fs from 'fs'

// 添加Logo
const logoBase64 = fs.readFileSync('./assets/logo.png', 'base64')
doc.addImage(logoBase64, 'PNG', 10, 10, 30, 10)

// 添加页眉
doc.setFontSize(10)
doc.text('超级洞察 - AI内容策略平台', 50, 15)

// 添加页脚（每页）
for (let i = 1; i <= doc.internal.getNumberOfPages(); i++) {
  doc.setPage(i)
  doc.text(`第 ${i} 页`, 100, 290)
  doc.text('© 特赞科技 2026', 150, 290)
}
```

**用户价值**:
- ✅ 品牌一致性提升
- ✅ 专业性增强
- ✅ 客户报告更正式

**预估时长**: 2-3小时
- Logo集成: 1h
- 页眉页脚: 1h
- 样式调整: 1h

**技术风险**: 低
- jsPDF支持图片和文字
- 无数据迁移
- 无前端改动

**决策**: 延后至Phase 6（可选）
- 优先完成搜索体验优化
- PDF品牌化作为锦上添花

---

#### 6. 前端UI测试（优先级：🟢 低）

**问题**: 
- v2.2.2 Phase 3延后的功能
- 无Lighthouse性能测试
- 无浏览器兼容性测试

**解决方案**:
- 使用Playwright执行Lighthouse测试
- 验证Chrome/Firefox/Safari兼容性
- 性能优化建议

**用户价值**:
- ✅ 性能优化
- ✅ 浏览器兼容性保证
- ✅ 用户体验提升

**预估时长**: 4-5小时

**决策**: 延后至v2.29.0
- 优先级低，不影响核心功能
- 需要投入较多时间

---

#### 7. 用户反馈系统（优先级：🟢 低）

**问题**: 
- v2.11.0 Phase 1延后的功能
- 无法收集用户反馈
- 产品迭代缺少用户输入

**解决方案**:
- 在应用中添加反馈按钮
- 收集用户评分和文字反馈
- 存储到feedback表

**用户价值**:
- ✅ 用户声音被听到
- ✅ 产品迭代有数据支撑

**预估时长**: 3-4小时

**决策**: 延后至v2.29.0
- 优先级低，不影响核心功能

---

## 🎯 v2.28.0 Phase规划

### Phase分解策略

基于以下原则：
1. **优先技术债务** - Repository测试补充（质量保障）
2. **聚焦用户体验** - 中文分词+搜索建议（搜索体验优化）
3. **快速交付** - 分阶段交付，避免单一Phase耗时过长
4. **可选增强** - PDF品牌化和API测试作为可选Phase

### Phase 1: Repository测试补充（核心Repository）

**目标**: 补充最重要的3个Repository单元测试

**范围**:
- ✅ userRepo - 10个测试用例（用户管理核心）
- ✅ projectRepo - 10个测试用例（项目管理核心）
- ✅ insightRepo - 8个测试用例（洞察生成核心）

**不包含**:
- notificationSettingsRepo（已有功能验证，优先级低）
- topicRepo（简单CRUD，优先级低）
- scriptRepo（简单CRUD，优先级低）

**测试用例详细**:

**userRepo (10个测试用例)**:
1. `create()` - 创建用户
2. `findByEmail()` - 根据邮箱查找
3. `findById()` - 根据ID查找
4. `findAll()` - 查找所有用户
5. `update()` - 更新用户信息
6. `updatePassword()` - 更新密码
7. `verifyEmail()` - 邮箱验证
8. `delete()` - 删除用户
9. `count()` - 统计用户数量
10. 边界测试 - 邮箱重复、密码格式验证

**projectRepo (10个测试用例)**:
1. `create()` - 创建项目
2. `findById()` - 根据ID查找
3. `findByUser()` - 根据用户查找
4. `findAll()` - 查找所有项目
5. `update()` - 更新项目信息
6. `updateStats()` - 更新项目统计
7. `delete()` - 删除项目（级联删除验证）
8. `count()` - 统计项目数量
9. `timeline()` - 项目时间线
10. 边界测试 - 项目名称重复、时间线记录

**insightRepo (8个测试用例)**:
1. `create()` - 创建洞察
2. `findById()` - 根据ID查找
3. `findByProject()` - 根据项目查找
4. `update()` - 更新洞察
5. `updateStatus()` - 更新状态
6. `delete()` - 删除洞察
7. `count()` - 统计洞察数量
8. 边界测试 - 状态转换、批量创建

**预估时长**: 4-5小时
- userRepo: 1.5h
- projectRepo: 1.5h
- insightRepo: 1h
- 边界测试: 1h

**输出**:
- `server/db/repositories/user.repo.test.ts`
- `server/db/repositories/project.repo.test.ts`
- `server/db/repositories/insight.repo.test.ts`

**验证**:
```bash
npm test user.repo.test.ts
npm test project.repo.test.ts
npm test insight.repo.test.ts
```

---

### Phase 2: 中文分词支持

**目标**: 集成jieba分词器，提升中文搜索体验

**技术方案**: 预分词存储（方案1）

**步骤**:

**2.1 jieba集成**
```bash
# 安装依赖
npm install nodejieba
```

**2.2 创建分词工具函数**
```typescript
// server/utils/tokenizer.ts
import jieba from 'nodejieba'

/**
 * 中文分词工具
 * @param text 原始文本
 * @returns 分词后的文本（空格分隔）
 */
export function tokenizeZh(text: string): string {
  const segments = jieba.cut(text)
  return segments.join(' ')
}

// 示例
// tokenizeZh('这个产品的卖点很独特') → '这个 产品 的 卖点 很 独特'
```

**2.3 修改commentRepo.create()**
```typescript
// server/db/repositories/comment.repo.ts
import { tokenizeZh } from '../../utils/tokenizer.js'

export const commentRepo = {
  create(input: CreateCommentInput): Comment {
    const db = getDb()
    const id = genId()
    const now = Date.now()

    // 原有逻辑创建评论
    const comment: Comment = { /* ... */ }
    
    // 插入comments表
    db.prepare('INSERT INTO comments (...) VALUES (...)').run(/* ... */)
    
    // 手动插入FTS5表（分词后）
    const segmented = tokenizeZh(comment.content)
    db.prepare('INSERT INTO comments_fts(comment_id, content) VALUES (?, ?)').run(id, segmented)
    
    return comment
  }
}
```

**2.4 修改Migration 16（Trigger改为手动插入）**
```typescript
// 删除INSERT Trigger（改为手动插入）
// db.exec('CREATE TRIGGER comments_fts_insert ...')

// 保留UPDATE和DELETE Trigger
db.exec(`
  CREATE TRIGGER IF NOT EXISTS comments_fts_update AFTER UPDATE ON comments
  BEGIN
    UPDATE comments_fts SET content = (SELECT tokenize_zh(NEW.content)) WHERE comment_id = OLD.id;
  END
`)
```

**问题**: SQLite Trigger无法调用Node.js函数
**解决方案**: Trigger仅处理UPDATE和DELETE，INSERT由应用层处理

**2.5 数据迁移（重新分词所有评论）**
```typescript
// server/db/migrations.ts - Migration 17
console.log('[Migration] Re-tokenizing all comments with jieba (v2.28.0 Phase 2)')

// 清空FTS5表
db.exec('DELETE FROM comments_fts')

// 重新分词并插入
const comments = db.prepare('SELECT id, content FROM comments').all() as Array<{ id: string, content: string }>

const insertStmt = db.prepare('INSERT INTO comments_fts(comment_id, content) VALUES (?, ?)')
for (const comment of comments) {
  const segmented = tokenizeZh(comment.content)
  insertStmt.run(comment.id, segmented)
}

console.log(`[Migration] Re-tokenized ${comments.length} comments`)
```

**预估时长**: 2-3小时
- jieba集成: 1h
- commentRepo改造: 0.5h
- Migration 17: 0.5h
- 测试验证: 1h

**验证**:
```bash
# 测试分词效果
node -e "const jieba = require('nodejieba'); console.log(jieba.cut('这个产品的卖点很独特'))"
# 输出: [ '这个', '产品', '的', '卖点', '很', '独特' ]

# 测试搜索
curl -X POST http://localhost:3001/api/comments/search -d '{"keyword":"产品"}'
# 应该能匹配到"产品卖点"
```

**技术风险**: 中
- jieba依赖安装（可能需要native编译）
- Trigger无法调用Node.js函数（需要应用层处理INSERT）
- 数据迁移耗时（评论量大时）

---

### Phase 3: 搜索建议（自动补全）

**目标**: 基于历史记录实现搜索建议

**前端实现**:

**3.1 修改CommentSearchModal.tsx**
```typescript
// src/components/comments/CommentSearchModal.tsx
import { useState, useEffect, useRef } from 'react'

export function CommentSearchModal() {
  const [keyword, setKeyword] = useState('')
  const [suggestions, setSuggestions] = useState<Array<{
    keyword: string
    search_count: number
  }>>([])
  const [showSuggestions, setShowSuggestions] = useState(false)
  const [selectedIndex, setSelectedIndex] = useState(-1)
  const inputRef = useRef<HTMLInputElement>(null)

  // 获取搜索建议
  useEffect(() => {
    if (keyword.length > 0) {
      fetch(`/api/comments/search/history?limit=10`)
        .then(res => res.json())
        .then(data => {
          const filtered = data.filter(h => h.keyword.includes(keyword))
          setSuggestions(filtered)
          setShowSuggestions(filtered.length > 0)
        })
    } else {
      setShowSuggestions(false)
    }
  }, [keyword])

  // 键盘导航
  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (!showSuggestions) return

    switch (e.key) {
      case 'ArrowDown':
        e.preventDefault()
        setSelectedIndex(prev => (prev + 1) % suggestions.length)
        break
      case 'ArrowUp':
        e.preventDefault()
        setSelectedIndex(prev => (prev - 1 + suggestions.length) % suggestions.length)
        break
      case 'Enter':
        e.preventDefault()
        if (selectedIndex >= 0) {
          handleSelectSuggestion(suggestions[selectedIndex].keyword)
        } else {
          handleSearch()
        }
        break
      case 'Escape':
        setShowSuggestions(false)
        break
    }
  }

  // 选择建议
  const handleSelectSuggestion = (suggestion: string) => {
    setKeyword(suggestion)
    setShowSuggestions(false)
    // 自动执行搜索
    handleSearch(suggestion)
  }

  return (
    <div className="relative">
      <input
        ref={inputRef}
        type="text"
        value={keyword}
        onChange={(e) => setKeyword(e.target.value)}
        onKeyDown={handleKeyDown}
        placeholder="搜索评论..."
        className="w-full px-4 py-2 border rounded-lg"
      />

      {showSuggestions && (
        <div className="absolute top-full left-0 right-0 mt-1 bg-white border rounded-lg shadow-lg z-10">
          <div className="px-3 py-2 text-xs text-gray-500">最近搜索</div>
          {suggestions.map((suggestion, index) => (
            <div
              key={suggestion.keyword}
              className={`px-4 py-2 cursor-pointer hover:bg-gray-100 ${
                index === selectedIndex ? 'bg-gray-100' : ''
              }`}
              onClick={() => handleSelectSuggestion(suggestion.keyword)}
            >
              <div className="flex items-center justify-between">
                <span>{suggestion.keyword}</span>
                <span className="text-xs text-gray-400">({suggestion.search_count}次)</span>
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

**预估时长**: 1-2小时
- 前端组件改造: 1h
- 键盘交互: 0.5h
- UI样式调整: 0.5h

**验证**:
- 输入"产"，应显示包含"产"的历史搜索（"产品"、"产品卖点"等）
- ↓键应选择下一个建议
- Enter应自动搜索
- Esc应关闭建议列表

**技术风险**: 低
- API已存在（GET /api/comments/search/history）
- 前端逻辑简单
- 无数据库改动

---

### Phase 4: 测试与文档归档

**目标**: 完成v2.28.0开发总结和发布准备

**任务**:
1. ✅ 执行所有测试用例（28个单元测试）
2. ✅ 验证中文分词效果
3. ✅ 验证搜索建议功能
4. ✅ 更新package.json（v2.27.0 → v2.28.0）
5. ✅ 创建v2.28.0-RELEASE-NOTES.md
6. ✅ 创建WORK-SUMMARY-v2.28.0.md
7. ✅ 更新CHANGELOG.md

**测试验证**:
```bash
# 运行所有测试
npm test

# 预期结果
Test Files  4 passed (4)
     Tests  38 passed (38)  # commentRepo(10) + userRepo(10) + projectRepo(10) + insightRepo(8)

# 验证中文分词
curl -X POST http://localhost:3001/api/comments/search -d '{"keyword":"产品"}'
# 应该能匹配到"产品卖点"

# 验证搜索建议
curl http://localhost:3001/api/comments/search/history?limit=10
# 应该返回历史搜索记录
```

**预估时长**: 1-2小时

---

### Phase 5: API集成测试（可选）

**目标**: 补充完整的API集成测试

**范围**:
- POST /api/comments/search - 搜索评论
- GET /api/comments/search/history - 搜索历史
- DELETE /api/comments/search/history - 清空历史
- POST /api/comments - 创建评论
- PUT /api/comments/:id - 更新评论
- DELETE /api/comments/:id - 删除评论

**预估时长**: 3-4小时

**决策**: 可选（如时间充裕则执行）

---

### Phase 6: PDF导出品牌化（可选）

**目标**: 为PDF添加Logo和页眉页脚

**预估时长**: 2-3小时

**决策**: 可选（如时间充裕则执行）

---

## ⏱️ 时间估算

### 必选Phase（核心功能）

| Phase | 任务 | 预估时长 | 累计时长 |
|-------|-----|---------|---------|
| Phase 0 | 产品规划 | 0.5h | 0.5h |
| Phase 1 | Repository测试补充 | 4-5h | 5h |
| Phase 2 | 中文分词支持 | 2-3h | 8h |
| Phase 3 | 搜索建议 | 1-2h | 10h |
| Phase 4 | 测试与文档归档 | 1-2h | 12h |

**总计（必选）**: 10-12小时（2-3天）

### 可选Phase（增强功能）

| Phase | 任务 | 预估时长 |
|-------|-----|---------|
| Phase 5 | API集成测试 | 3-4h |
| Phase 6 | PDF导出品牌化 | 2-3h |

**总计（可选）**: 5-7小时

**完整v2.28.0**: 15-19小时（3-4天）

---

## 🎯 技术决策

### 决策1: Repository测试范围

**问题**: 是否补充所有46个测试用例？

**方案对比**:
- **方案A**: 全部补充（6个Repository，46个测试用例）
  - ✅ 测试覆盖率最高
  - ❌ 耗时5-6小时，单一Phase过长
  - ❌ 优先级不均衡（notificationSettingsRepo优先级低）

- **方案B**: 补充核心Repository（3个Repository，28个测试用例）**（推荐）**
  - ✅ 覆盖最重要的3个Repository（userRepo/projectRepo/insightRepo）
  - ✅ 耗时4-5小时，合理
  - ✅ 剩余3个可延后至v2.29.0
  - ❌ 测试覆盖率略低（73% vs 100%）

**决策**: 方案B（核心Repository优先）
- userRepo/projectRepo/insightRepo是核心功能，必须有测试覆盖
- notificationSettingsRepo/topicRepo/scriptRepo优先级低，可延后
- 保持开发节奏，避免单一Phase耗时过长

---

### 决策2: 中文分词方案

**问题**: 预分词存储 vs 查询时分词？

**方案对比**:
- **方案A**: 预分词存储**（推荐）**
  - ✅ 搜索性能最优（无需每次分词）
  - ✅ 利用FTS5内置索引
  - ❌ 存储空间增加10-20%
  - ❌ Trigger无法调用Node.js函数（需要应用层处理INSERT）

- **方案B**: 查询时分词
  - ✅ 无需修改存储
  - ✅ Trigger无需改动
  - ❌ 每次搜索都要分词（性能略低）
  - ❌ 无法利用FTS5内置分词

**决策**: 方案A（预分词存储）
- 搜索性能最优（用户体验第一）
- 存储空间增加可接受（评论数据量不大）
- Trigger问题可通过应用层处理INSERT解决

---

### 决策3: 搜索建议触发时机

**问题**: 何时显示搜索建议？

**方案对比**:
- **方案A**: 输入时实时显示**（推荐）**
  - ✅ 用户体验最好（即时反馈）
  - ✅ 符合用户习惯（Google搜索）
  - ❌ 可能触发过多API请求

- **方案B**: 点击输入框时显示
  - ✅ 减少API请求
  - ❌ 用户体验略差（需要点击）

- **方案C**: 输入1-2个字符后显示
  - ✅ 平衡体验和性能
  - ❌ 实现复杂度略高

**决策**: 方案A（输入时实时显示）
- 用户体验第一
- 历史记录API性能高（仅查询search_history表）
- 可通过debounce优化（300ms延迟）

---

### 决策4: 可选Phase执行策略

**问题**: API集成测试和PDF品牌化是否执行？

**决策**: 根据Phase 1-4完成情况决定
- 如果Phase 1-4完成顺利（≤10小时），执行Phase 5（API集成测试）
- 如果时间充裕（≤15小时），执行Phase 6（PDF品牌化）
- 如果时间不足（>12小时），延后至v2.29.0

**优先级排序**:
1. Phase 1-4（必选）- 搜索体验优化
2. Phase 5（优先可选）- API集成测试
3. Phase 6（次优可选）- PDF品牌化

---

## 📈 预期成果

### 技术成果
- ✅ **测试覆盖率提升** - 38个单元测试（commentRepo 10 + userRepo 10 + projectRepo 10 + insightRepo 8）
- ✅ **中文搜索体验** - "产品"能匹配"产品卖点"
- ✅ **搜索效率提升** - 历史记录自动补全
- ✅ **技术债务清理** - 核心Repository有测试保护

### 用户价值
- 🔍 **搜索精准度** - 中文分词，搜索更智能
- ⚡ **搜索效率** - 自动补全，减少输入
- 🧪 **系统稳定性** - 测试覆盖，质量保障
- 📊 **产品完整度** - 95% → 98%

### 产品指标
- 搜索成功率: 80% → 95%（中文分词提升）
- 搜索效率: 平均输入字符数减少30%（自动补全）
- 测试覆盖率: 10 → 38个单元测试
- 技术债务: 46个待测试 → 18个待测试（减少61%）

---

## 🚀 v2.29.0预留功能

基于v2.28.0完成情况，以下功能延后至v2.29.0：

**优先级高**:
1. **剩余Repository测试** - notificationSettingsRepo/topicRepo/scriptRepo（18个测试用例）
2. **API集成测试** - 如v2.28.0未完成
3. **PDF导出品牌化** - 如v2.28.0未完成

**优先级中**:
4. **前端UI测试** - Lighthouse + 浏览器兼容性（v2.2.2遗留）
5. **用户反馈系统** - v2.11.0遗留

**优先级低**:
6. **性能优化** - 大数据量下的搜索性能
7. **搜索高级功能** - 布尔运算、短语搜索

**预计开发时长**: 8-10小时（2天）

---

## 📝 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|-----|------|------|---------|
| jieba依赖安装失败 | 🔴 高 | 🟡 中 | 提供预编译二进制，文档详细安装步骤 |
| Trigger无法调用Node.js | 🟡 中 | 🔴 高 | 应用层处理INSERT，仅Trigger处理UPDATE/DELETE |
| 数据迁移耗时过长 | 🟢 低 | 🟢 低 | 批量处理，显示进度条 |
| 测试用例编写耗时 | 🟡 中 | 🟡 中 | 复用测试框架，模板化测试用例 |

### 进度风险

| 风险 | 影响 | 概率 | 缓解措施 |
|-----|------|------|---------|
| Phase 1耗时超预期 | 🟡 中 | 🟡 中 | 优先完成userRepo/projectRepo，insightRepo可延后 |
| Phase 2 jieba集成复杂 | 🔴 高 | 🟢 低 | 提前测试jieba安装，准备备选方案 |
| Phase 5-6时间不足 | 🟢 低 | 🟡 中 | 作为可选Phase，延后至v2.29.0 |

---

## 📖 参考文档

- [v2.27.0产品规划](./v2.27.0-PRODUCT-PLAN.md) - FTS5搜索规划
- [v2.27.0发布说明](./v2.27.0-RELEASE-NOTES.md) - FTS5技术文档
- [v2.27.0工作总结](./WORK-SUMMARY-v2.27.0.md) - 开发总结
- [nodejieba文档](https://github.com/yanyiwu/nodejieba) - 中文分词库
- [SQLite FTS5文档](https://www.sqlite.org/fts5.html) - 全文搜索引擎

---

**规划者**: Claude Opus 4.6  
**规划日期**: 2026-04-12  
**规划版本**: v2.28.0  
**预计开发时长**: 10-12小时（必选Phase），15-19小时（完整版本）

**下一步**: 进入Phase 1 - Repository测试补充（userRepo/projectRepo/insightRepo）
