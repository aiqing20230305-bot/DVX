# v2.35.0 部署记录

**部署时间**: 2026-04-15 17:23  
**提交哈希**: `59d8f3d`  
**分支**: DVX  
**部署人**: 张经纬 + Claude Code  
**状态**: ✅ 已提交到本地仓库

---

## 📦 本次部署内容

### 核心修复 (Production-Critical)
1. ✅ Word文档上传支持 (.doc, .docx)
2. ✅ Modal输入框焦点跳转问题（阻断性）
3. ✅ 选题选择状态丢失问题（阻断性）
4. ✅ 开发环境权限403错误（阻断性）
5. ✅ UI主题恢复为白色（亮色）

### UI优化
6. ✅ 统一配色为Linear风格（紫色系）
7. ✅ 移除审批管理功能

---

## 📊 变更统计

```
674 files changed, 401098 insertions(+), 94 deletions(-)
```

**注意**: 大部分新增行数来自测试覆盖率报告文件（coverage/），实际核心代码变更约 200 行。

### 核心文件变更
```
package.json                                (+1)    # 添加mammoth依赖
server/middleware/permission.middleware.ts  (+20)   # 新增checkPermission函数
server/services/parser/word-parser.ts       (新增)  # Word解析器
src/store/topic.store.ts                    (+21)   # 状态同步优化
src/components/shared/Modal.tsx             (+19/-13) # 焦点管理修复
```

---

## 🧪 测试验证

### 已通过测试 ✅
- [x] Word文档上传（.doc, .docx）
- [x] 选题勾选 → 切换到脚本页面 → 状态保持
- [x] Modal输入框连续输入无焦点跳转
- [x] 批量选题操作（选择/删除/优先级）
- [x] 开发环境所有权限操作正常
- [x] UI主题为白色（亮色）
- [x] 配色统一为Linear风格

### 待生产验证
- [ ] Word文档大文件上传（>10MB）
- [ ] 并发上传多个Word文档
- [ ] 长时间会话选题状态保持

---

## 🚀 部署步骤

### 1. 安装新依赖
```bash
npm install
```
**预期结果**: 成功安装 `mammoth@^1.8.0`

### 2. 重启开发服务器
```bash
# 停止当前服务
Ctrl+C

# 重新启动
npm run dev
```

### 3. 验证关键功能
- [ ] 上传Word文档测试
- [ ] 选题勾选状态测试
- [ ] Modal输入框测试

---

## 📁 部署文件清单

### 新增文件
- `server/services/parser/word-parser.ts` - Word文档解析器
- `v2.35.0-RELEASE.md` - 版本发布说明
- `v2.35.0-DEPLOY.md` - 本文件
- `coverage/*` - 测试覆盖率报告（可选，不影响功能）

### 修改文件（核心）
- `package.json`
- `server/middleware/permission.middleware.ts`
- `server/middleware/upload.middleware.ts`
- `server/routes/insight.route.ts`
- `server/routes/script.route.ts`
- `server/routes/topic.route.ts`
- `server/routes/upload.route.ts`
- `src/App.tsx`
- `src/components/layout/Sidebar.tsx`
- `src/components/shared/Modal.tsx`
- `src/components/workbench/DropZone.tsx`
- `src/pages/Scripts.tsx`
- `src/store/topic.store.ts`
- `src/store/ui.store.ts`
- `src/styles/globals.css`

---

## ⚠️ 注意事项

### 1. 开发环境权限
- 开发环境（`NODE_ENV !== 'production'`）下所有权限检查自动绕过
- 生产环境需要正确配置用户权限

### 2. 选题状态说明
- 用户勾选的选题是**会话级状态**（存储在前端 store）
- 刷新页面后状态会丢失（除非点击"标记为已选择"保存到数据库）
- 切换页面（选题↔脚本）状态会保持

### 3. Word文档限制
- 仅支持文本提取
- 不支持表格、图片、复杂格式
- 单个文件建议不超过 10MB

### 4. Coverage文件
- `coverage/` 目录包含测试覆盖率报告
- 可以选择不提交到远程仓库（添加到 .gitignore）
- 不影响功能运行

---

## 🔄 回滚方案

如需回滚到上一个稳定版本：

```bash
# 查看提交历史
git log --oneline

# 回滚到上一个提交
git reset --hard 2d54f40

# 或创建新分支保留当前状态后回滚
git branch backup-v2.35.0
git reset --hard 2d54f40
```

---

## 📈 下一步计划

### 短期（v2.35.1）
- [ ] 添加Word文档解析单元测试
- [ ] 优化大文件上传进度显示
- [ ] 添加 coverage/ 到 .gitignore

### 中期（v2.36.0）
- [ ] Word文档表格解析支持
- [ ] 选题状态持久化优化
- [ ] SSE连接稳定性改进

---

## 📞 问题联系

**技术问题**: dev@example.com  
**产品问题**: 张经纬  
**紧急问题**: 直接回滚到上一版本

---

**部署确认**: ✅ 已完成本地提交，等待推送到远程仓库
