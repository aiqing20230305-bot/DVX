# v2.14.1 Product Planning - Script Template System Enhancement

**Version**: v2.14.1  
**Theme**: Complete Scripts Page Template Integration  
**Planning Date**: 2026-04-12  
**Estimated Duration**: 1.5 hours  
**Priority**: P1 (High - Completes v2.14.0 functionality)  
**Status**: 待审批

---

## Executive Summary

v2.14.0完成了脚本模板系统的核心功能（Templates页面），但Scripts页面的模板集成仍为简化版（仅跳转按钮）。v2.14.1将完成完整的Scripts页面集成，使用户能够：
1. **在Scripts页面直接选择模板**（无需跳转）
2. **将现有脚本保存为模板**（反向工作流）
3. **快速复用成功脚本**（提升效率）

---

## Background

### v2.14.0 Current State

**已完成**:
- ✅ /templates页面：完整的模板库界面
- ✅ TemplateCard：模板卡片展示
- ✅ TemplateDetailModal：模板详情预览
- ✅ VariableFormModal：变量填写表单
- ✅ Scripts页面："从模板创建"按钮（简化版）

**限制**:
- ❌ Scripts页面点击"从模板创建"会跳转到/templates页面（离开当前工作流）
- ❌ 无法将现有脚本保存为模板（单向流程）
- ❌ 无法快速复用成功的脚本（需要手动复制或重新填写）

### User Pain Points

**场景1: 创建新脚本时想用模板**
- 当前: Scripts页面 → 点击"从模板创建" → 跳转/templates → 选择模板 → 填写变量 → 跳回/scripts
- 痛点: 流程断裂，需要跨页面跳转
- 期望: 在Scripts页面直接弹窗选择模板，填完直接生成

**场景2: 发现某个脚本效果好，想保存为模板**
- 当前: 无法操作（需要手动去/templates创建或找管理员）
- 痛点: 好的脚本无法快速复用，只能手动复制文案
- 期望: 右键或菜单"保存为模板"，自动提取结构，下次直接用

**场景3: 团队协作，快速复用成功案例**
- 当前: 成功的脚本只能看，无法变成可复用资产
- 痛点: 知识沉淀困难，每次都要从头写
- 期望: 一键转模板，团队共享，提升整体效率

---

## Product Goals

### Primary Goals

1. **完整的Scripts页面模板集成**
   - 用户无需离开Scripts页面即可使用模板创建脚本
   - 流畅的模态框交互体验

2. **脚本→模板的反向流程**
   - 让成功的脚本能够快速转化为可复用模板
   - 自动提取segments结构，减少手动操作

3. **提升团队协作效率**
   - 积累项目内的优质脚本模板
   - 降低新人上手门槛

### Success Metrics

- ✅ 模板使用率提升30%（相比v2.14.0的跳转方式）
- ✅ 自定义模板数量增长到50+（第一个月）
- ✅ Scripts页面停留时长增加20%（减少跳转）
- ✅ 用户反馈："更流畅"/"更方便"评价≥90%

---

## Feature Breakdown

### Feature 1: TemplateSelectModal（模板选择弹窗）

**Priority**: P0 (Must Have)  
**Estimated Time**: 30 minutes

#### Description

在Scripts页面点击"从模板创建"时，弹出模态框展示模板列表，用户可直接选择并填写变量，生成脚本后自动刷新Scripts列表。

#### User Flow

```
Scripts页面
  ↓ 点击"从模板创建"按钮
TemplateSelectModal（模态框）
  ├─ 左侧：模板列表（简化版TemplateCard）
  │  - 显示模板名称、分类badge、使用次数
  │  - 支持快速筛选（分类/平台）
  │  - 点击模板展开详情
  ├─ 右侧：模板详情预览
  │  - 显示segments结构
  │  - 显示变量列表
  └─ 底部：操作按钮
     - 取消（关闭模态框）
     - 使用模板（打开VariableFormModal）
```

#### UI Design

**Modal Size**: XL (90% viewport width, max 1200px)

**Layout**:
```
┌───────────────────────────────────────────────┐
│ 选择模板                               [X]    │
├──────────────┬────────────────────────────────┤
│ 模板列表     │ 模板详情                       │
│ (左侧40%)    │ (右侧60%)                      │
│              │                                │
│ 搜索框       │ 情感共鸣型-室友对比            │
│ ┌─────────┐ │ 分类: 情感型 | 平台: 抖音      │
│ │ [筛选]  │ │                                │
│ └─────────┘ │ 脚本结构:                      │
│              │ • Hook: {产品名}真的{卖点}!    │
│ ┌─────────┐ │ • Solution: ...                │
│ │情感型   │ │ • Proof: ...                   │
│ │快节奏   │ │                                │
│ │(+4)     │ │ 需要填写变量:                  │
│ └─────────┘ │ • 产品名、价格_高、价格_低...  │
│              │                                │
│ ┌─────────┐ │                                │
│ │理性型   │ │                                │
│ │数据背书 │ │                                │
│ └─────────┘ │                                │
├──────────────┴────────────────────────────────┤
│                     [取消]  [使用模板]         │
└───────────────────────────────────────────────┘
```

#### Technical Implementation

**Component**: `src/components/scripts/TemplateSelectModal.tsx`

**Key Features**:
1. **复用TemplateCard组件**（简化版）
   - 移除hover actions
   - 添加selected状态
   - 点击选中 + 右侧预览联动

2. **复用TemplateDetailModal的预览逻辑**
   - 提取预览部分为独立组件`TemplatePreview.tsx`
   - TemplateSelectModal右侧使用此组件

3. **集成VariableFormModal**
   - 点击"使用模板"打开VariableFormModal
   - 生成成功后关闭所有模态框
   - 刷新Scripts列表

**State Management**:
```typescript
interface TemplateSelectModalProps {
  isOpen: boolean
  onClose: () => void
  onScriptCreated: () => void // 回调：刷新Scripts列表
}
```

**API Calls**:
- GET /api/templates - 加载模板列表
- GET /api/templates/:id - 加载模板详情（选中时）
- POST /api/templates/:id/apply - 应用模板（通过VariableFormModal）

---

### Feature 2: SaveAsTemplateModal（保存为模板弹窗）

**Priority**: P0 (Must Have)  
**Estimated Time**: 30 minutes

#### Description

用户可将现有脚本（尤其是效果好的）保存为模板，供后续复用。自动提取segments结构，用户只需填写模板名称、描述、分类等元数据。

#### User Flow

```
Scripts页面
  ↓ 点击ScriptCard右上角菜单 → "保存为模板"
SaveAsTemplateModal（模态框）
  ├─ 模板信息表单
  │  - 模板名称*（必填）
  │  - 模板描述*（必填）
  │  - 分类*（情感型/理性型/种草型/自定义）
  │  - 平台*（抖音/快手/小红书）
  │  - 标签（可选，多个）
  ├─ Segments预览
  │  - 显示自动提取的脚本结构
  │  - 用户可微调（替换部分文本为变量）
  └─ 操作按钮
     - 取消
     - 保存模板
```

#### UI Design

**Modal Size**: Large (70% viewport width, max 900px)

**Layout**:
```
┌─────────────────────────────────────────┐
│ 保存为模板                       [X]    │
├─────────────────────────────────────────┤
│ 基本信息                                │
│ ┌─────────────────────────────────────┐ │
│ │ 模板名称* [_____________________]  │ │
│ │ 模板描述* [_____________________]  │ │
│ │ 分类*     [▼情感型]                │ │
│ │ 平台*     [▼抖音]                  │ │
│ │ 标签      [快节奏] [+添加]         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 脚本结构预览                            │
│ ┌─────────────────────────────────────┐ │
│ │ Segment 1 (Hook, 0-3s)             │ │
│ │ 文案: "室友花299元买的洗发水..."    │ │
│ │ 镜头: "宿舍场景，产品对比特写"      │ │
│ │ [变量化部分文本]                    │ │
│ ├─────────────────────────────────────┤ │
│ │ Segment 2 (Problem, 3-8s)          │ │
│ │ ...                                 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 💡 提示：保存后，该模板将仅在当前项目可用│
│                                         │
├─────────────────────────────────────────┤
│                   [取消]  [保存模板]    │
└─────────────────────────────────────────┘
```

#### Technical Implementation

**Component**: `src/components/scripts/SaveAsTemplateModal.tsx`

**Key Features**:
1. **自动提取segments**
   - 从script.segments JSON提取
   - 保留type/timing/voiceover/shot/duration

2. **变量化辅助**（v2.14.1简化版，可选）
   - 高亮识别可能的变量（如数字、产品名）
   - 用户点击文本片段 → 转换为{变量名}

3. **表单验证**
   - 必填字段：名称、描述、分类、平台
   - 名称不能与现有模板重复（项目内）

**State Management**:
```typescript
interface SaveAsTemplateModalProps {
  scriptId: string
  isOpen: boolean
  onClose: () => void
  onSuccess: () => void // 回调：成功后提示并关闭
}
```

**API Calls**:
- GET /api/scripts/:id - 加载脚本详情（获取segments）
- POST /api/templates - 创建新模板

**Backend API** (需新增):
```typescript
POST /api/templates
Body: {
  name: string
  description: string
  category: 'emotion' | 'rational' | 'harvest' | 'custom'
  platform: 'douyin' | 'kuaishou' | 'xiaohongshu'
  segments: TemplateSegment[]
  tags: string[]
  projectId: string
  sourceScriptId: string // 来源脚本ID
}
```

---

### Feature 3: ScriptCard Menu Integration（ScriptCard菜单集成）

**Priority**: P0 (Must Have)  
**Estimated Time**: 20 minutes

#### Description

在ScriptCard右上角添加三点菜单，包含"保存为模板"选项。

#### UI Design

**Before** (v2.14.0):
```
┌────────────────────────────┐
│ 多芬洗发水脚本 (A版)       │
│ 30秒 | 168字               │
│ 2026-04-12                 │
│                            │
│ 室友花299元买的洗发水...   │
└────────────────────────────┘
```

**After** (v2.14.1):
```
┌────────────────────────────┐
│ 多芬洗发水脚本 (A版)  [⋮] │ ← 新增三点菜单
│ 30秒 | 168字               │
│ 2026-04-12                 │
│                            │
│ 室友花299元买的洗发水...   │
└────────────────────────────┘
```

**Menu Items**:
```
┌──────────────┐
│ 保存为模板   │ ← 新增（打开SaveAsTemplateModal）
│ 编辑脚本     │ ← 已有功能
│ 删除脚本     │ ← 已有功能
└──────────────┘
```

#### Technical Implementation

**Component**: `src/components/scripts/ScriptCard.tsx`（修改）

**Changes**:
1. 添加Menu组件（使用`@headlessui/react`的`Menu`或自定义）
2. 右上角添加三点图标按钮（MoreVertical from lucide-react）
3. Menu打开时显示3个选项
4. 点击"保存为模板"触发`onSaveAsTemplate`回调

**Props Update**:
```typescript
interface ScriptCardProps {
  script: Script
  onEdit: (id: string) => void
  onDelete: (id: string) => void
  onSaveAsTemplate: (id: string) => void // 新增
}
```

**Parent Component** (Scripts.tsx) **Changes**:
```typescript
const handleSaveAsTemplate = (scriptId: string) => {
  setSelectedScriptId(scriptId)
  setSaveAsTemplateModalOpen(true)
}

// In JSX:
<ScriptCard
  script={script}
  onEdit={handleEdit}
  onDelete={handleDelete}
  onSaveAsTemplate={handleSaveAsTemplate} // 新增
/>

<SaveAsTemplateModal
  scriptId={selectedScriptId}
  isOpen={saveAsTemplateModalOpen}
  onClose={() => setSaveAsTemplateModalOpen(false)}
  onSuccess={() => {
    toast.success('模板创建成功', '已保存到模板库')
    setSaveAsTemplateModalOpen(false)
  }}
/>
```

---

## Technical Architecture

### Component Hierarchy

```
src/pages/Scripts.tsx
├─ ScriptCard.tsx (修改)
│  └─ Menu（三点菜单）
│     └─ "保存为模板"选项
├─ TemplateSelectModal.tsx (新增)
│  ├─ TemplateCard.tsx（复用，简化版）
│  ├─ TemplatePreview.tsx（提取自TemplateDetailModal）
│  └─ VariableFormModal.tsx（复用）
└─ SaveAsTemplateModal.tsx (新增)
   └─ SegmentsList（Segments预览）
```

### Shared Components

**提取公共组件** (提升复用):
1. `TemplatePreview.tsx` - 模板详情预览
   - 当前在TemplateDetailModal中
   - 提取后供TemplateSelectModal右侧使用

2. `SegmentItem.tsx` - 单个Segment展示
   - 显示type/timing/content/direction
   - 高亮变量语法

### API Endpoints

**已有**:
- GET /api/templates
- GET /api/templates/:id
- GET /api/templates/stats
- POST /api/templates/:id/apply

**新增**:
- POST /api/templates - 创建模板（脚本转模板）

### Data Flow

```
TemplateSelectModal:
  GET /api/templates
    → 展示列表
    → 选中模板 → GET /api/templates/:id
    → 点击"使用模板" → 打开VariableFormModal
    → VariableFormModal提交 → POST /api/templates/:id/apply
    → 生成成功 → 关闭所有modal → 刷新Scripts列表

SaveAsTemplateModal:
  GET /api/scripts/:id
    → 提取segments
    → 用户填写表单
    → POST /api/templates
    → 成功 → Toast提示 → 关闭modal
```

---

## Implementation Plan

### Phase 1: TemplateSelectModal (30分钟)

**Sub-tasks**:
1. 提取TemplatePreview组件 (10分钟)
   - 从TemplateDetailModal.tsx提取预览部分
   - 创建`src/components/shared/TemplatePreview.tsx`
   - 接受template作为props

2. 创建TemplateSelectModal (15分钟)
   - 左侧：模板列表（复用TemplateCard简化版）
   - 右侧：TemplatePreview
   - 底部：操作按钮

3. 集成到Scripts页面 (5分钟)
   - 修改"从模板创建"按钮onClick
   - 添加modal状态管理
   - 连接onScriptCreated回调

---

### Phase 2: SaveAsTemplateModal (30分钟)

**Sub-tasks**:
1. 创建后端API (10分钟)
   - `server/routes/template.route.ts`
   - POST /api/templates endpoint
   - 验证：名称不重复、必填字段

2. 创建SaveAsTemplateModal组件 (15分钟)
   - 表单：名称、描述、分类、平台、标签
   - Segments预览
   - 提交逻辑

3. 集成到Scripts页面 (5分钟)
   - 添加modal状态管理
   - 连接ScriptCard的onSaveAsTemplate回调

---

### Phase 3: ScriptCard Menu (20分钟)

**Sub-tasks**:
1. 修改ScriptCard组件 (10分钟)
   - 添加Menu组件
   - 三点图标按钮
   - Menu items: 编辑、删除、保存为模板

2. Scripts页面集成 (5分钟)
   - 添加handleSaveAsTemplate函数
   - 传递给ScriptCard

3. 测试完整流程 (5分钟)
   - 点击菜单 → 打开SaveAsTemplateModal
   - 填写表单 → 保存成功 → 验证模板创建

---

### Phase 4: Testing & Polish (10分钟)

**Testing**:
1. TemplateSelectModal测试
   - 列表加载
   - 模板选择 + 预览联动
   - 使用模板 → 生成脚本

2. SaveAsTemplateModal测试
   - Segments自动提取
   - 表单验证
   - 模板创建成功

3. 端到端测试
   - Scripts页面 → 从模板创建 → 生成脚本
   - Scripts页面 → 保存为模板 → 验证模板库

**Polish**:
- Toast提示优化
- Loading状态优化
- Error handling

---

## Timeline & Resource

### Estimated Timeline

| Phase | Duration | Cumulative |
|-------|----------|------------|
| Phase 1: TemplateSelectModal | 30分钟 | 30分钟 |
| Phase 2: SaveAsTemplateModal | 30分钟 | 1小时 |
| Phase 3: ScriptCard Menu | 20分钟 | 1小时20分钟 |
| Phase 4: Testing & Polish | 10分钟 | 1小时30分钟 |
| **Total** | **1.5小时** | **1.5小时** |

### Resource Requirements

- **Developer**: 1 (Full-stack, React + Express.js)
- **Dependencies**: 无新增依赖
- **Database**: 无新表，使用现有script_templates表

---

## Risks & Mitigation

### Risk 1: TemplateSelectModal UX Complexity (Low)

**Risk**: 左右分栏 + 嵌套modal可能让用户confused

**Mitigation**:
- 清晰的视觉分割（border + background color）
- 左侧选中状态明显（highlight background）
- 右侧预览跟随滚动

### Risk 2: SaveAsTemplateModal Segments Extraction (Low)

**Risk**: 脚本segments结构可能不规范，导致提取失败

**Mitigation**:
- 增加segments格式验证
- 如果提取失败，显示友好错误提示
- 允许用户手动调整segments（v2.14.2增强）

### Risk 3: API Performance (Very Low)

**Risk**: 创建模板时如果segments很大，可能超时

**Mitigation**:
- Segments JSON通常<5KB，性能无问题
- 添加请求超时提示（5秒）

---

## Success Criteria

### Functional Requirements

- ✅ TemplateSelectModal能正常展示模板列表和预览
- ✅ 从TemplateSelectModal选择模板能成功生成脚本
- ✅ SaveAsTemplateModal能正确提取segments
- ✅ 通过SaveAsTemplateModal创建的模板能在Templates页面显示
- ✅ ScriptCard菜单能正常打开并触发SaveAsTemplateModal

### Non-Functional Requirements

- ✅ Modal打开/关闭动画流畅（<200ms）
- ✅ API响应时间<500ms
- ✅ 表单验证实时反馈
- ✅ Toast提示友好清晰

### User Experience

- ✅ 用户无需离开Scripts页面即可使用模板
- ✅ 用户能快速将成功脚本保存为模板
- ✅ 整体流程流畅，无断裂感

---

## Post-Launch Plan

### Monitoring (第一周)

- 监控TemplateSelectModal打开率
- 监控SaveAsTemplateModal使用率
- 收集用户反馈："是否比跳转方式更方便？"

### Iteration (v2.14.2)

**Potential Enhancements**:
1. SaveAsTemplateModal增强
   - 变量化辅助工具（智能识别可变部分）
   - Segments手动编辑功能

2. TemplateSelectModal增强
   - 最近使用模板（快速访问）
   - 收藏模板功能

3. 批量操作
   - 批量保存多个脚本为模板
   - 批量应用模板到多个选题

---

## Comparison: v2.14.0 vs v2.14.1

| Feature | v2.14.0 | v2.14.1 |
|---------|---------|---------|
| 模板浏览 | ✅ /templates页面 | ✅ 同v2.14.0 |
| 模板应用 | ✅ Templates页面填写变量 | ✅ + Scripts页面内弹窗 |
| 脚本转模板 | ❌ 无法操作 | ✅ ScriptCard菜单 |
| 用户体验 | 🔄 需要跨页面跳转 | ✅ 全程在Scripts页面 |
| 模板积累 | 仅官方3个 | 用户自创N个 |
| 团队协作 | 低（手动复制脚本） | 高（模板共享） |

---

## Decision & Next Steps

### Recommendation

**强烈建议立即启动v2.14.1开发**，原因：
1. 完成v2.14.0预留功能，形成完整闭环
2. 用户体验显著提升（无需跨页面跳转）
3. 开启"脚本→模板"反向流程，激活模板生态
4. 时间成本低（仅1.5小时）

### Approval Needed

- [ ] Product Owner审批
- [ ] 技术负责人审批
- [ ] 优先级确认（是否优先于pending tasks #398, #486, #505）

### Next Steps (If Approved)

1. 创建开发任务
   - Task: v2.14.1 Phase 1 - TemplateSelectModal
   - Task: v2.14.1 Phase 2 - SaveAsTemplateModal
   - Task: v2.14.1 Phase 3 - ScriptCard Menu
   - Task: v2.14.1 Phase 4 - Testing & Polish

2. 更新CHANGELOG
   - 新增v2.14.1条目（待开发）

3. 开始开发
   - 预计完成时间：1.5小时后

---

## Appendix

### User Feedback from v2.14.0 (Hypothetical)

- "模板功能很好，但每次都要跳转到另一个页面有点麻烦" → v2.14.1解决
- "我写了很多好脚本，想保存下来复用，但不知道怎么操作" → v2.14.1解决
- "希望能直接在Scripts页面选模板，不要打断我的工作流" → v2.14.1解决

### Technical Debt

- 无新增技术债
- 复用现有组件（TemplateCard, TemplatePreview, VariableFormModal）
- 代码结构清晰，易于维护

---

**Planning Document Version**: 1.0  
**Status**: ✅ Planning Complete - Awaiting Approval  
**Next Action**: Review & Approve → Start Development
